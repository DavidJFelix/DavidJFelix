# 2026-05-15 Progress

## Summary

Project created. Goal: move unattended LLM-driven GitHub Actions (freshness cron, PR review) off Anthropic-billed Claude onto a cheaper runtime — `pi-core` (on bun) or `opencode`. Interactive Claude Code work stays on Claude.

## Decisions

- In-scope workflows: `cron_dependency_freshness.yml` and `bot_claude_code_review.yml`.
- Out of scope for now: `bot_claude.yml` (interactive mention bot stays on Claude; revisit later).
- Skills are the portable unit — the freshness skill in particular was written to be runtime-agnostic and serves as the canary for the migration.

## Work completed

- Created `plan.md` with goal, rationale, in-scope workflows, and phased implementation.
- Added entry to `docs/projects.md` under Priority 1 — Monorepo Hygiene.

## Next steps

- Phase 1: evaluate `pi-core` (bun) vs `opencode` vs other viable runtimes against the criteria in `plan.md` (skill compatibility, tool access, model backend flexibility, maturity, maintenance cost).

## Open questions

- Does `pi-core` or `opencode` understand the `.claude/skills/` format, or how much porting is required per skill?
- Which model backend(s) are we targeting on the new runtime?
