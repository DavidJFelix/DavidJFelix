# 2026-04

## 2026-04-29

### test(djf.io): add Playwright E2E suite and CI gating

Stood up Playwright in `apps/djf.io` with smoke coverage of home, blog index, MDX post rendering, tags, and RSS. Added `.github/workflows/djf-io-ci.yml` with lint+build and E2E jobs (path-filtered, mise-managed toolchain, Playwright browsers cached).

### test(djf.io): add Vitest unit suite (schema + components)

Added Vitest with Astro's `getViteConfig` so tests can import `astro:content` virtuals and `.astro` components directly. Coverage includes the blog content schema (required fields, date coercion, optional shapes) and `BaseLayout`/`BlogPost` rendered via `experimental_AstroContainer`. CI gains a `unit` job.

## 2026-04-28

### docs(repo): add nine new projects

Added project plans and kickoff progress notes for: blog-style-improvement, djf-io-testing, cloudflare-migration, new-domain-sites, dotfiles-overhaul, sentry-integration, dependency-freshness, profile-bio-refresh, and posthog-integration. Updated `docs/projects.md` index.
