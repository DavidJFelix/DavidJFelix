# 2026-06-18 — kickoff

New project from two CI pain points David flagged: too many workflows run on a change, and installs
(notably Playwright) are slow. A full survey of `.github/workflows/` grounds the scope in plan.md.
David's steer: fix "too many actions" with **better filtering**, not concurrency.

## Next

- Trim spurious shared paths (`.config/cspell.json`, `.prettierrc.json`) from the 10 per-app CI
  filters; path-filter `ci-spell.yml`; drop redundant CD self-references.
- Decide with David how the shared toolchain/lint configs (`biome.jsonc`, `.oxlintrc.json`,
  `mise.toml`) should trigger — keep the fan-out, or move lint-rule validation to one repo-wide job.
- Cache the pnpm store in CI; make the web-session Playwright install non-blocking/lazy.
