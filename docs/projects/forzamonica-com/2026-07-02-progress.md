# forzamonica.com — 2026-07-02 progress

## Session summary

Resumed the parked project. Re-checked the two human blockers: the domain one is cleared (#223
closed 2026-06-14; David reports forzamonica.com is attached to the worker), the Shopify store one
(#222) remains open, so the storefront stays on mock.shop. Built the store site map and shared
layout: about page, policy stubs, styled 404, footer, header cart badge, per-page titles, and e2e
coverage for all of it.

## Work completed

- `/about` placeholder page and three policy stubs (`/policies/shipping`, `/policies/returns`,
  `/policies/privacy`) via a shared `PolicyArticle` component.
- Styled 404 (`notFoundComponent` on the root route), SSR'd with a 404 status, wrapped in the shop
  chrome.
- `SiteFooter` (Shop / Company / Policies columns + copyright); root route restructured into shell +
  layout component with a flex-grow main so the footer sits at the viewport bottom.
- Header: About link and a cart-count badge driven by a root-route loader calling the new
  `fetchCartQuantity` server function (badge-sized `CART_QUANTITY_QUERY`; no cookie, no round trip).
  Cart mutations already `router.invalidate()`, which keeps the badge in sync.
- Per-page titles on every route (product detail titles from loader data).
- e2e: 2 -> 6 tests; selectors scoped to banner/contentinfo now that link names repeat across header
  and footer; home visual baseline re-authored for the new chrome.
- `playwright.config.ts`: optional `PLAYWRIGHT_CHROMIUM_EXECUTABLE` launch override for sandboxes
  where the pinned browser download is blocked (used it this session; CI path unchanged).

## Decisions

- **Flat catalog, no collections routes yet** (David, this session): nav leads with a single Shop
  page until the real product line exists; collections become worthwhile at cutover.
- **Brand stays placeholder** (David, this session): red/neutral tokens remain; about/policy copy is
  written to be replaced.
- Policy copy states plainly that no orders are live yet rather than faking terms; real policies
  come from the Shopify store (#222).

## Next steps

- Shopify store + Storefront token (#222) stays the gate for the real-catalog cutover.
- Candidate next tasks from the plan: brand/theme pass once direction exists, extend e2e to the
  catalog -> cart flow, collections/search when the catalog warrants.

## Blockers / open questions

- #222 (Shopify store + token) — human task, unchanged.
- `wrangler.toml` still carries the commented-out `routes` block while David reports the domain is
  attached (presumably via the Cloudflare dashboard). Config and reality should be reconciled --
  restoring the block is a one-line change once confirmed.
