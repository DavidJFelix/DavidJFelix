# 2026-06

## 2026-06-25

### feat(djf.io): add a custom 404 page

The site had no custom not-found page, so unmatched routes fell back to Cloudflare's default. Added
`src/pages/404.astro` -- a centered "404 / Page not found" built on the same `BaseLayout`, PandaCSS
tokens, and JetBrains Mono accent the rest of the site uses, with a "Back home" link. Astro
prerenders it to `dist/client/404.html`, which the `@astrojs/cloudflare` adapter serves for any
unmatched route with a genuine 404 status (verified against the built worker via `wrangler dev`:
`/` returns 200, an unknown path returns 404 serving the page). Co-located `_404.e2e.test.ts`
asserts the 404 status, the page title, and the home link.

## 2026-06-23

### feat(djf.io): tunnel Sentry events through an on-demand API route

The client SDK added yesterday POSTs envelopes straight to `*.ingest.sentry.io`, which ad/tracker
blockers drop -- so a chunk of real-user errors never arrive. Fixed it with a tunnel, per Sentry's
Astro guide: the browser SDK now sends envelopes to a same-origin path (`tunnel: '/bugs'`) and the
site forwards them to Sentry's ingest API server-side. The tunnel is a real Astro API endpoint
(`src/pages/bugs.ts`, `prerender = false`) via the `@astrojs/cloudflare` adapter -- not a hand-rolled
worker fronting the site. Every page stays prerendered and is served straight from assets; Cloudflare
invokes the worker only for this one route. The relay logic stays in the unit-tested,
100%-covered `src/lib/sentry-tunnel.ts` (forwards only to a genuine Sentry ingest host, and -- given
the site's own DSN, inlined at build via `import.meta.env.PUBLIC_SENTRY_DSN` -- only for that
project).

Making the adapter fit a sharp-using static site took three settings: `prerenderEnvironment: 'node'`
(prerender in Node, since the OG cards and image optimization use native `sharp`, which can't load in
workerd, the adapter's default prerender environment); a no-op session driver
(`sessionDrivers.null()`, so the adapter doesn't default to a KV-backed driver that would need a
`SESSION` KV namespace); and `security.checkOrigin: false` (the endpoint is a stateless relay with
its own validation, and Astro's CSRF check would otherwise 403 the SDK's `text/plain` envelope POST).
`@sentry/cloudflare` is added as a build-only devDependency because `@sentry/astro` hard-requires it
whenever a Cloudflare adapter is present; the server SDK stays off, so it's never bundled into the
worker. The adapter emits its deployable config at `dist/server/wrangler.json` (assets ->
`dist/client`), so deploy and the per-PR preview point at it (the shared `preview-wrangler` action
gained an opt-in `wrangler-config` input); local e2e boots `wrangler dev` on it. Added deterministic
e2e exercising the route's guards (405 on GET, 400 on a bad/missing DSN) without ever reaching
Sentry; preview and CI ship no DSN, so the
project pin is inert and the suite stays deterministic and secret-free.

### feat(djf.io): add PostHog product analytics behind a same-origin proxy endpoint

First app in the `posthog-integration` rollout. djf.io stays static (every page prerenders), and
it rides the `@astrojs/cloudflare` adapter already added for the Sentry tunnel -- giving it a
second on-demand route, `src/pages/ingest/[...path].ts`, that reverse-proxies PostHog at request
time. Analytics events ride this first-party `/ingest` path instead of `*.posthog.com`, so content
blockers that drop the PostHog domain can't drop our events. The endpoint routes `/static` and
`/array` to PostHog US's assets host and everything else to ingestion (a pure, 100%-covered
helper, `src/lib/posthog-proxy.ts`), forces HTTPS upstream, forwards `CF-Connecting-IP` as
`X-Forwarded-For`, strips request cookies, drops any upstream `Set-Cookie`, and times out the
upstream fetch at 10s (a clean 504 if PostHog stalls).

The proxy needs no new adapter config -- it's just another `prerender = false` route on the worker
the Sentry tunnel already stood up (prerender in Node so the OG route's `sharp`/`satori` run at
build, build-time image optimization, a no-op session driver, and `@sentry/cloudflare` for the
adapter's server build). The only build-side change is `posthog-js` as a dependency.

The browser SDK (`posthog-js`) is gated like Sentry: `BaseLayout` only renders the snippet when
`PUBLIC_POSTHOG_KEY` is set at build, so local, CI, preview, and e2e builds ship zero PostHog code
and stay deterministic (verified: keyless build emits no `posthog` reference; 25 Playwright tests
incl. the visual baseline pass, plus co-located `_[...path].e2e.test.ts` cases that exercise the
proxy against the deployed preview -- where `/ingest/static/array.js` and `/ingest/flags` serve
PostHog's SDK and API 200). Cookieless --
`persistence: 'memory'`, `person_profiles: 'identified_only'`, autocapture and session replay off,
pageview + pageleave on -- so no consent banner is needed; `api_host` is the live origin's `/ingest`
and `ui_host` is `us.posthog.com` so the toolbar still links correctly. The deploy workflow wires
`PUBLIC_POSTHOG_KEY` from a `POSTHOG_KEY_DJF_IO` repo variable (the project key is public by design,
not a secret), left unset until the PostHog project exists (issue #95), so deploys stay dark until
then. `wrangler deploy --dry-run` bundles the worker + assets with only the `ASSETS` binding; the
live upstream round-trip can only be exercised on Cloudflare's edge.

### fix(repo): green up calendar-visualizer + davidjfelix.com biome under the cspell fan-out

Editing the shared `.config/cspell.json` (the PostHog spell-words) fans CI out to every app via its
`.config/**` path filter, which surfaced two latent biome failures in apps this PR never touched: a
"used" `Calendar` import flagged unused in calendar-visualizer's `.astro`, and `noSvgWithoutTitle` on
both apps' `public/favicon.svg`. Fixed at the source, not by silencing: biome lints only an `.astro`
file's frontmatter, not its template, so a symbol used only in markup reads as unused -- that one
false positive is now turned off **once**, centrally, in the root `biome.jsonc` for `**/*.astro`
(`astro check`/tsgo is template-aware and owns Astro unused detection), and the per-app `*.astro`
overrides -- including djf.io's pre-existing one -- are removed. The favicons get a `role="img"` +
`aria-label` (like djf.io's) instead of excluding `*.svg`. CONTRIBUTING now bans both shortcuts.

## 2026-06-22

### feat(djf.io): add client-side Sentry error + performance monitoring

djf.io is a fully static site -- every route is resolved at build time -- so it gets client-only
Sentry: unhandled-error capture plus browser performance tracing via `@sentry/astro`, with no adapter
and no server SDK. The browser SDK is bundled only when `PUBLIC_SENTRY_DSN` is set (production
deploy), and source maps upload only when the auth token, org, and project are all present, so local,
CI, preview, and e2e builds ship zero Sentry code and stay deterministic. Backend/Worker
instrumentation is deferred to apps with a real runtime (f311x leads that pattern).

## 2026-06-21

### refactor(djf.io): rewrite search as a native Astro component, drop React

Search was the site's only React island, and a `client:load` island is the wrong tool for it: a
window-level `useEffect` for the ⌘K hotkey, hydration-readiness markers for the e2e tests, and a
post-hydration `useEffect` that swapped the `<kbd>` hint from `Ctrl K` to `⌘K` (the visible flash on
Mac). Rewrote it as `Search.astro` — a native `<dialog>` (which already gives focus trapping,
`Escape`, and `closedby="any"` light dismiss) plus one small module script for open/close, the
global hotkey, and the Pagefind query. The platform hint is now pure CSS off a `data-platform`
attribute that an inline `<head>` script in `BaseLayout` sets before first paint, so the right key
shows immediately with no flash and no React state. Removing the island let `@astrojs/react`,
`react`, `react-dom`, and `@types/react-dom` go (the OG route keeps `@types/react` for the `ReactNode`
type), and the `react()` integration and the container-renderer plumbing in the layout tests are
gone. No React ships to the browser on any page. Search e2e (including non-Mac/Mac hint coverage)
and the layout unit tests still pass.

## 2026-06-18

### fix(djf.io): standard.site document.site must be the publication AT-URI

A document record's `site` field must be the publication's AT-URI
(`at://{did}/site.standard.publication/{rkey}`); a bare url like `https://djf.io` is rejected by
the validator as malformed. The prior cut wrote `site` as the publication url to key ownership off
the url, but that produced invalid records. The sync now writes `site: publicationUri()` (the
AT-URI), and the build resolver scopes documents to djf.io by first finding the publication whose
`url` is `https://djf.io` (so ownership is still anchored on the url, not the record key) and then
matching documents whose `site` equals that publication's AT-URI. Other publications sharing the
repo stay excluded.

### fix(djf.io): standard.site records must use TID record keys

The `site.standard.publication` and `site.standard.document` lexicons require TID record keys
(13-char base32-sortable; validity is purely syntactic), so the first cut's human-readable keys —
`self` for the publication and the post slug for documents — were rejected by the standard.site
validator. Publication keys are now a committed server-assigned TID constant (`PUBLICATION_RKEY`),
so `publicationUri()` stays synchronous and offline. Document keys are server-assigned: the sync
script `createRecord`s new documents (the PDS assigns the TID) and matches existing ones by their
`path` to update in place, and the site resolves each post's `at://` URI from the PDS at build time
(`src/lib/standard-site-resolve.ts`, one memoized fetch). That resolution is gated behind a CD-only
`STANDARD_SITE_RESOLVE` flag and degrades to warn-and-omit, so local/CI/e2e/preview builds stay
offline and deterministic (the document `<link>` is omitted; the publication link is always present
via the constant). The CD deploy runs the sync before `astro build` so records exist when the build
resolves them. The sync is non-destructive (create/update only); migrating or pruning legacy
records is a manual step. The reconcile decision is a pure, unit-tested planner; the networked
resolver is excluded from lib coverage. Documents are scoped to djf.io by their `site` field (the
one publication whose url is `https://djf.io`), so another publication sharing the repo is never
matched or resolved. Re-validate at site-validator.fly.dev.

### feat(djf.io): standard.site / AT Protocol compatibility; close standard-site-compat

Made djf.io a first-class long-form publisher on the AT Protocol (the network behind Bluesky)
and closed `docs/projects/standard-site-compat/`. A shared, framework-agnostic
`src/lib/standard-site.ts` holds the public DID (`did:plc:nlbldots3jn3lk6mzca4rqzm`, verified
`alsoKnownAs: at://djf.io`), publication metadata (reusing the RSS strings), and the rkey/URI
helpers. Record keys are deterministic — publication rkey `self`, document rkey = the post
slug — so every `at://` URI is computable from that one DID; the static build emits the same
URIs the sync writes, with nothing persisted back to the repo.

Website artifacts: two `.well-known` endpoints — `site.standard.publication` (the publication
AT-URI) and `atproto-did` (the bare DID, an HTTP handle-verification path for `@djf.io` that
complements the DNS `_atproto` record) — plus `<link rel="site.standard.*">` tags on the base
layout and every post, covered by unit (100% lib coverage) and Playwright e2e tests.

Go-live: `bin/sync-standard-site.ts` (bun, `mise run sync-standard-site`) logs in to bsky.social
with an app password, asserts the session DID before any write, and idempotently upserts one
publication plus one document per post via `putRecord`. The CD workflow gained a guarded sync
step between `astro build` and `wrangler deploy`, gated on `ATPROTO_APP_PASSWORD` (an encrypted
Actions secret, #249) so it skips when unset and otherwise blocks the deploy on sync success —
the site never ships referencing records that don't exist. Running `mise run` in that job also
required `MISE_TRUSTED_CONFIG_PATHS`, matching `ci-djf-io.yml`. The app password grants full
write to the repo; it is secret-stored, rotatable, and fenced by the session-DID guard.
Post-deploy confirmation is the standard.site validator at site-validator.fly.dev.

### test(repo): close testing-harness — layered safety net in place

Closed `docs/projects/testing-harness/` after Phases 0–4 landed a fast unit → smoke → e2e →
coverage net that runs in web sessions, not just CI. Phase 0: a `SessionStart` hook
(`.claude/hooks/session-start.ts`, bun) installs the mise toolchain and every app's dependencies on
a fresh web container, so mise tasks and smoke boots behave as they do in CI. Phase 1: shared checks
in `bin/smoke-checks.ts` drive both `bin/smoke-test.ts` (post-deploy, prod URLs) and
`bin/smoke-local.ts` (pre-merge), and `mise run smoke` boots f311x's production worker under workerd
via `wrangler dev` and asserts hydration + the chat echo loop — wired as a PR job so "deployed but
dead" fails the gate. Phase 2: the `smoke` task contract was documented in CLAUDE.md and rolled to
the other deployed apps (calendar-visualizer, davidjfelix.com → `astro preview`; ravrun →
`vite preview`); djf.io keeps its Playwright e2e suite as the heavier gate. Phase 3: djf.io's blog
list/tag logic was extracted to a pure `src/lib/blog.ts` and covered by `src/lib/blog.test.ts`
(11 tests; 44 unit tests total). Phase 4: a monorepo aggregator (`bin/run-app-tasks.ts`) behind root
`mise run test` / `check` fans a task across every app, and vitest v8 coverage gates the pure logic
of f311x, djf.io, calendar-visualizer, and forzamonica.com (100% on the gated modules, ratcheted via
`--coverage`). The two residual papercuts — the pnpm no-TTY purge abort and web-session Playwright
install speed — were intentionally dropped; the Playwright-install wait is picked up by the new
`ci-pipeline-efficiency` project.

### chore(tooling): close linter-formatter-standardization — standard in place

Closed `docs/projects/linter-formatter-standardization/`; the JS/TS standard is set and documented.
Biome owns lint+format for code/JSON/CSS/framework files, Oxlint adds rules, tsgo type-checks,
Prettier is scoped to Markdown/MDX only (`proseWrap: always`), and cspell is the single repo-wide
spell gate. The mechanical slice (2026-06-16) made cspell a root tool (`mise run spell`, enforced by
`ci-spell.yml`) and removed the redundant per-app spell scripts, fixed the non-functional root
`.prettierignore` to the `**` + `!*/` recipe, bumped davidjfelix.com and djf.io to Biome 2.4.16, and
added `docs/tooling-standard.md`. Phase 3b (earlier) had every app declare
`typecheck`/`lint`/`format`/`test`/`build` as mise tasks with per-app paths-filtered CI. The three
open scope decisions were resolved at close: Rust is **scoped out** (exercise code only; no
clippy/rustfmt/CI until a real Rust project exists); the legacy JS dirs (Joy-of-React,
Advent-of-Code 2020 TypeScript) **will be folded into the standard at low priority**; and all of
`docs/` **will be Prettier-formatted with a `prettier --check` guard** (superseding the
changelog-only option). Those two concrete cleanups, plus the `.config/cspell.json` →
`.config/cspell.jsonc` rename, respawned as the `lint-format-loose-ends` project.

### chore(repo): close dependency-freshness — freshness skill + Renovate adoption in place

Closed `docs/projects/dependency-freshness/`. Phases 1–4 shipped a reusable freshness skill
(`.claude/skills/dependency-freshness/`) that discovers, evaluates, applies, and PRs updates across
npm, mise, and Cargo — patch+minor batched one PR per ecosystem, majors and known-incompatible bumps
split out — triggered weekly by `cron-dependency-freshness.yml` and on demand by the
`/dependency-freshness` slash command, with a `deps:needs-attention` escalation label as the single
review surface. The 2026-06-11 decision handed ecosystem ownership to **Renovate** (npm + mise +
Cargo, plus `lockFileMaintenance` for transitive drift), so the skill no longer owns manifest bumps.
The residual — extend Renovate repo-wide, confirm Dependabot is off, pin the `latest`-tagged tsgo
deps, revisit gated auto-merge now that previews shipped, and **retire the freshness skill + cron +
slash command** once Renovate coverage is confirmed — respawned as the `renovate-rollout` project.

### docs(repo): reconcile the project portfolio; respawn residuals, add CI-efficiency + blog-content

Trued `docs/projects.md` up to reality and reprioritized. Replaced the stale 2026-06-11 priority
tiers (whose premise — "f311x is broken; build preview/observability infra" — was overtaken by the
2026-06-14 prod restore and the 2026-06-17 preview rollout) with status-grouped sections
(Active / Blocked / Parked / Deferred) ordered by priority, and gave every project an honest status
instead of a blanket "In Progress." Closed three finished projects into this changelog
(testing-harness, linter-formatter-standardization, dependency-freshness) and respawned their
residuals as tightly-scoped projects (`lint-format-loose-ends`, `renovate-rollout`). Reshaped f311x
(real model + error visibility via Sentry; per-PR preview marked done), promoted Sentry and PostHog
to active full-fleet rollouts, rescoped LLM Automation Migration to the review bot alone, and fixed
the dangling `preview-deployments` links. Added two new projects from David's direction:
`ci-pipeline-efficiency` (tighten CI trigger filtering so only the right workflows run; cache the
pnpm store and fix the blocking web-session Playwright install) and `blog-content` (begin writing
djf.io posts, first up "attention is all you need").

## 2026-06-17

### feat(preview): per-PR preview deployments for every wrangler app; close preview-deployments

Generalized the per-PR preview pipeline from f311x (its Alchemy-based Phase 1 proving ground —
preview deploy + smoke + screenshot on every PR) to the nine wrangler-deployed apps:
calendar-visualizer, djf.io, ravrun, davidjfelix.com, onvibes.org, revision.city, startchi.com,
monicandavid.com, and pkg.dog. Each PR touching an app now uploads a non-active preview version,
smokes the preview URL, screenshot-diffs it against a committed baseline, and upserts a sticky
comment with the URL.

The wrangler shape is simpler than f311x's Alchemy stage. `wrangler versions upload --preview-alias
pr-<N>` uploads a non-active version with a deterministic
`pr-<N>-<worker>.<subdomain>.workers.dev` URL; because it never applies triggers, production routes
and custom domains stay on the active version — no route-hijack risk and nothing to tear down
(Cloudflare prunes old aliases), so there is no `closed`/destroy job unlike f311x. Custom-domain
apps set `preview_urls = true` in their `wrangler.toml` so the alias URL resolves.

The nine apps share one implementation: a composite action `.github/actions/preview-wrangler/`
(build → upload → URL smoke → Playwright screenshots → sticky comment) invoked by thin per-app
`cd-preview-<app>.yml` callers, plus three repo-root bin scripts — `bin/upload-preview.ts` (the
versions-upload + deterministic-URL resolver, with a unit-tested parser gated by a new
`mise run test:bin` job in `ci-repo.yml`), `bin/smoke-url.ts` (the remote sibling of each app's
`bin/smoke-local.ts`: 200 + complete HTML document + first hashed asset serves), and
`bin/comment-preview.ts` (generalized from f311x). A composite action was chosen over a reusable
`workflow_call` workflow to match the existing `setup-mise` precedent and run in the app directory.

Screenshots run on every app, not just where Playwright already existed: each gets a committed
Linux/chromium `toHaveScreenshot` baseline and a `PREVIEW_URL` seam that points the suite at the
deployed preview in CI and at a local production boot when authoring baselines. The local boot
mirrors each app's smoke — `astro`/`vite preview` for the static and TanStack Start apps (the latter
serving the SSR worker in workerd via `@cloudflare/vite-plugin`), and config-driven `wrangler dev`
for the SvelteKit and Nuxt apps; djf.io reuses its existing behavioral Playwright suite via the same
seam.

Live-deploy verification is pending on the first real PR (the upload path needs CI secrets), and the
preview/smoke/screenshot results feeding the dependency-freshness auto-merge bar are tracked in that
project. With the infrastructure complete, this closes out `docs/projects/preview-deployments/`.
forzamonica.com is deferred to the forzamonica-shop project (its storefront pages depend on external
mock.shop data, which complicates a stable baseline).

### fix(repo): scope the mise CI cache key to the lockfile via a shared `setup-mise` action

`jdx/mise-action`'s default cache key hashes *all* mise config files repo-wide — its
`{{file_hash}}` globs `**/mise.toml` — so editing any app's `mise.toml` (even just adding
a task) invalidates the one shared key for every workflow. The #247 smoke-task commit did
exactly that, so every CI job started on a cold cache; dozens of parallel jobs then
stampeded the same key, and two unlucky ones (monicandavid `svelte-check`, pkg.dog
`format check`) stalled on the cold Node-tarball download until their 10-minute timeout
cancelled them. Factored the mise setup into a local composite action
`.github/actions/setup-mise` that pins the cache key to the real tool inputs —
`mise-v1-{{platform}}-${{ hashFiles('.config/mise.toml', '.config/mise.lock') }}` — and
converted all 79 `jdx/mise-action` usages across 28 workflows to it. App `mise.toml` edits
no longer bust the cache; only genuine tool-version bumps (which change `.config/mise.lock`)
do. Also added `.github/actions/**` to `ci-actions-lint.yml`'s triggers so the shared
action is linted when it changes. The 11 `cd-deploy` jobs (previously `cache: false`, a
template default with no recorded rationale) now use the shared cache too — safe because
the key is lockfile-scoped (identical pinned tool binaries) and deploys run only on
`push: main`, a trusted context whose cache scope no untrusted PR can write to.

### test(apps): runtime smoke gates for the five deployed scaffolded sites

The five scaffolded sites (onvibes.org, revision.city, startchi.com, monicandavid.com,
pkg.dog) had shipped to Cloudflare since 2026-06-14 with no runtime gate — a violation of
the CLAUDE.md contract that every deployed app boot its production build locally and assert
the critical path serves, and exactly the "green CI, broken at runtime" class that took
f311x down in the 2026-06-11 incident. Each now has a `bin/smoke-local.ts`, a `smoke` mise
task (`depends = ["build"]`), and a CI `smoke` job mirroring the `vitest` one. The boot
method follows the stack: `astro preview` for the static onvibes.org; `vite preview` for
revision.city/startchi.com, whose `@cloudflare/vite-plugin` serves the SSR worker in workerd;
and a config-driven `wrangler dev` for monicandavid.com (SvelteKit) and pkg.dog (Nuxt),
because their framework `preview` commands either serve the app under Node (SvelteKit's
`vite preview`) or wrap wrangler in a child process that survives teardown (Nuxt's
`nuxt preview`) — booting the real `_worker.js` / `.output` artifact under workerd is the
only faithful gate, with the worker entry, assets, and compatibility settings read from each
app's `wrangler.toml`. The two `tsgo`-checked apps (revision.city, startchi.com) also gained
`@types/bun` so their now-typechecked `bin/` resolves the Bun globals, matching forzamonica.com.

## 2026-06-16

### chore(tooling): enforce cspell repo-wide; fix `.prettierignore`; bump Biome; add tooling standard

Landed the decision-free "mechanical slice" of the linter/formatter standardization
project. cspell was configured but enforced nowhere; it's now a single repo-wide gate —
a root tool (mise `npm:cspell`) run by `mise run spell` and gated on every push/PR by a
new `ci-spell.yml`, with the redundant (and in 6 of 11 apps, broken) per-app `spell`
scripts and cspell devDependencies removed. Clearing the backlog to green meant ignoring
generated/build trees (`.svelte-kit`, `.nuxt`, `.output`, …) and adding ~80 real domain
terms to the shared dictionary. The broken root `.prettierignore` (`*` + `!**/*.md`, which
can't re-include nested files) was rewritten to a working `**` + `!*/` recipe — a bare
`prettier .` now touches only md/mdx and descends the tree. davidjfelix.com and djf.io were
bumped off the stale Biome 2.4.4 schema to 2.4.16. Phase 4 docs added: `docs/tooling-standard.md`
(the per-ecosystem ownership map) and CLAUDE.md tooling entries for cspell, tsgo, and Rust.
The three scope decisions (Rust depth, legacy JS dirs, root-markdown enforcement) remain open.

## 2026-06-14

### fix(f311x): restore production — free domains from a leftover dev worker; smoke-test the chat loop

f311x had been serving a non-hydrating dev-mode page since the 2026-06-06 deploy. #221
(merged 2026-06-11) fixed the ingress, deploy-toolchain, and artifact bugs, but every prod
deploy afterward still failed at custom-domain reconciliation: a leftover dev-stage worker
(`f311x-website-dev-davidfelix-*`) still held `f311x.com` / `www.f311x.com`, and Cloudflare
won't attach one hostname to two Workers. Detaching the two domains from that worker let the
prod deploy (CD run 27504856773) claim ingress and ship the correct production bundle —
f311x.com and www now hydrate and the chat echo streams end to end. The #220 token-scope
worry was a red herring: the deploy authenticated fine and needed no zone scope. Also
hardened `bin/smoke-test.ts` to POST the chat endpoint and require the AG-UI echo stream
back, so a hydrated shell with a dead backend now fails the CD gate too (it previously
checked only hydration).

### fix(repo): let self-hosted Renovate run `mise lock`

The Renovate dependency dashboard was warning "mise lock was requested to run, but
mise is not permitted in the allowedUnsafeExecutions". Renovate's mise manager wants
to refresh our `mise.lock` files (`.config/mise.lock`, `Advent-of-Code/2023/mise.lock`)
by running `mise lock`, which it classifies as an unsafe execution and blocks unless
explicitly allowlisted. `allowedUnsafeExecutions` is a self-hosted-only option, so it
can't go in `.github/renovate.json`; set `RENOVATE_ALLOWED_UNSAFE_EXECUTIONS='["mise"]'`
in the `cron-renovate` workflow instead.

### chore(cd): deploy workflows for the five scaffolded sites

The five scaffolded domain sites (onvibes.org, revision.city, startchi.com,
monicandavid.com, pkg.dog) had CI but no deploy job; each now has a `cd-deploy-*`
workflow that ships to its workers.dev URL on push to `main`, mirroring the existing
CD template — pinned actions, mise, frozen `pnpm install`, then `wrangler deploy` via
the shared `CLOUDFLARE_API_TOKEN` / `CLOUDFLARE_ACCOUNT_ID` secrets. Triggers are
path-filtered to each app dir plus the workflow's own file. Closes the deploy gap left
when the apps were scaffolded.

### feat(apps): enable custom domain routes for the scaffolded sites

With the zones wired in the Cloudflare dashboard, the commented `custom_domain` routes
in each app's `wrangler.toml` were enabled so wrangler reconciles them on the next
deploy: onvibes.org, revision.city, startchi.com, and monicandavid.com (each apex +
`www`), plus pkg.dog (which also serves pkgdog.com — apex + `www` on both). Each domain
was probed over HTTP first (there is no Cloudflare API tool for custom domains):
onvibes.org, startchi.com, and the pkg.dog/pkgdog.com set returned the worker
(HTTP 200), while monicandavid.com (behind a Cloudflare challenge) and revision.city
(DNS still propagating) were enabled at David's direction. `www.pkg.dog` was added
during PR review.

## 2026-06-13

### feat(pkg.dog): move from Vue SPA to Nuxt; settle type-checker policy

`apps/pkg.dog` now runs on Nuxt 4 (Nitro `cloudflare_module` preset) instead of a bare
Vue 3 Vite SPA, giving it a server story like the repo's other meta-frameworks. PandaCSS
is wired through Nuxt's `postcss` config (there is no `@pandacss/nuxt` package), Ark UI
Vue stays as the component library, and the landing page is a single `app/app.vue`.
`nuxt build` emits the worker to `.output/server/index.mjs` with assets in
`.output/public`, matched by `wrangler.toml`; `wrangler deploy --dry-run` validates the
bundle and `ASSETS` binding.

This also settles the repo's type-checker policy: each framework uses its own native
checker, and tsgo (`@typescript/native-preview`) is reserved for the case where the
alternative would be bare `tsc`. So Nuxt uses `nuxt typecheck` (vue-tsc), and
`apps/monicandavid.com` (SvelteKit) was switched back from tsgo to `svelte-check`. The
TanStack Start / React apps keep tsgo (no framework-specific checker), and Astro keeps
`astro check`. `vue-tsc` is pinned to 3.3.4 to stay clear of pnpm's 24h release-age
cooldown without adding a `minimumReleaseAgeExclude` block.

## 2026-06-12

### feat(apps): scaffold five placeholder domain sites

New apps for the remaining new-domain-sites line items, each a minimal landing page
(title only) on the repo-standard toolchain with its own CI workflow:
`onvibes.org` (Astro), `monicandavid.com` (SvelteKit, switched from Astro),
`pkg.dog` (Vue 3 SPA), and `revision.city` + `startchi.com` (TanStack Start, following
the forzamonica pattern). PandaCSS with the default preset across the board; Ark UI is
installed in every non-Astro app as the blessed component library. Type-checking is
tsgo everywhere except Astro's `astro check` — `typescript`/`svelte-check`/`vue-tsc`
are deliberately absent until they support TS 7. No CD workflows or custom-domain
routes yet; each `wrangler.toml` carries a commented routes block for later.

### fix(forzamonica.com): deploy with shared Cloudflare credentials, workers.dev for now

The CD workflow now uses the shared `CLOUDFLARE_API_TOKEN` / `CLOUDFLARE_ACCOUNT_ID`
secrets like every other app instead of unprovisioned per-app `FORZAMONICA_*` secrets.
forzamonica.com is not registered yet, so the custom-domain `routes` were removed from
`wrangler.toml` (left as a comment to restore) and the worker serves from its
workers.dev URL until the domain exists.

## 2026-06-11

### feat(forzamonica.com): scaffold headless Shopify storefront

New app: `apps/forzamonica.com`, a headless Shopify storefront on TanStack Start +
Cloudflare Workers — the "eventual shop" line item from new-domain-sites growing into a
real project (`docs/projects/forzamonica-shop/`). Catalog, product detail, and cart
routes are fully working against mock.shop (Shopify's hosted mock Storefront API), so
the shop runs end to end with zero credentials; cutover to the real store is
config-only. All Storefront API calls go through server functions with the cart ID in
an httpOnly cookie, and checkout is Shopify-hosted. Two repo firsts: Ark UI styled via
PandaCSS slot recipes (`QuantityField` is the reference pattern), and TanStack Start
deployed with plain `wrangler deploy` via `@cloudflare/vite-plugin` (f311x uses
Alchemy). Per-app CI/CD workflows included; production deploy waits on two human-task
issues (Shopify store + token, Cloudflare secrets + zone).

### docs(docs): reprioritize project portfolio around app health and previews

Portfolio review session. f311x is broken in production despite green CI and a
successful deploy, so a new Priority 1 — App Health & Preview Infrastructure —
takes the top of `docs/projects.md`, with a new `preview-deployments` project
(per-PR preview deploys + smoke/screenshot tests; f311x is the proving
ground). Dependency-freshness gated auto-merge was deprioritized and its bar
raised (real test suites + preview verification; no GitHub required checks —
Renovate/GitHub per-PR auto-merge instead), and Renovate now owns all
dependency ecosystems including mise and Cargo, shrinking both the freshness
skill and the LLM-automation-migration scope (which is now explicitly
deferred). A current-state audit of linter-formatter-standardization found 2–4
sessions of real work remaining (Rust tooling at 0% across 22 crates, cspell
enforced nowhere, non-functional root `.prettierignore`, unscoped legacy
directories) with three scope decisions pending. Planning docs only — no
implementation.

### feat(djf.io): Pagefind search with Cmd/Ctrl+K dialog

Landed static search and closed out `docs/projects/djf-io-search/`. A small
inline integration in `astro.config.mjs` runs Pagefind over the built output
on `astro build`, emitting the index to `dist/pagefind/`. Only blog posts are
indexed (`data-pagefind-body` on the `BlogPost` article), so nav/footer chrome
and listing pages stay out of results. A new `Search` React island in the nav
-- the app's first -- opens a PandaCSS-styled native `<dialog>` via the button
or Cmd/Ctrl+K, queries the index with Pagefind's debounced search, and renders
titles plus highlighted excerpts for the top 8 results; Escape or a backdrop
click closes it. Under `astro dev` no index exists, so the dialog says search
needs a build instead of erroring. Because the nav now carries an island, the
container unit tests register the `@astrojs/react` renderer via
`loadRenderers`. Five new e2e tests cover search by title, search by body
text, the keyboard shortcut, Escape, and the empty state -- each first waits
for `astro-island[ssr]` to clear, since `client:load` islands attach their
listeners only after hydration and earlier keystrokes are dropped.

## 2026-06-10

### chore(repo): kebab-case all workflow filenames

PR review on #214 flagged that workflow filenames violated CLAUDE.md's
repo-wide kebab-case rule, and `docs/github-actions-style.md` turned out to
actively prescribe the conflicting snake_case convention. Decision: kebab-case
everywhere. Renamed all twelve pre-existing snake_case workflows (the three
introduced in #214 were already renamed during review), updated the style
guide's prefix taxonomy to `ci-` / `cd-` / `run-` / `cron-` / `bot-`, and
fixed references in zizmor/ghalint suppressions, the dependency-freshness
skill, and active project docs. Historical changelog entries and dated
progress notes keep the old names. Workflow run history starts fresh under
the new names; triggers, schedules, and suppressions are unaffected.

### chore(repo): biome formatting for repo-root config files

Root-owned config files (`biome.jsonc`, `.oxlintrc.json`, `.config/cspell.json`,
`.github/renovate.json`, `.vscode/*.json`, `.prettierrc.json`) were never
covered by any check — per-app CI only checks app subtrees. Formatted the two
drifted files, bumped the stale biome `$schema` pin, declared root
`format` / `format:fix` mise tasks in `.config/mise.toml` scoped to exactly
those files (apps shadow these tasks with their own), and added `ci-repo.yml`
running the check when any of them change. Legacy project dirs
(Advent-of-Code, Exercism, etc.) stay intentionally out of scope; repo-wide
cspell and root markdown formatting remain open Phase 3 items.

### fix(djf.io): render blog hero images again

Hero images defined in post frontmatter (`hero.image`) stopped displaying after the
blog migration: the content schema kept the field and posts still declared it
(`2023-12-30-shipposting.md` points at `src/assets/shipposting-hero.png`), but
`BlogPost.astro` never read `hero` from `post.data`, so the image was silently
dropped. The layout now renders `hero.image` via `astro:assets` `<Image>` beside
the post heading -- 160x160 with a 2x srcset for retina, vertically centered
against the title/meta block on wide screens and stacked above the title on
narrow ones, mirroring Starlight's old hero placement at a smaller size --
with rounded corners matching inline images and `loading="eager"` /
`fetchpriority="high"` since it sits above the fold.
Posts without a hero image (on-positivity, on-running) are unchanged. Added an e2e
test asserting the shipposting hero is visible and actually loads
(`naturalWidth` check).

### fix(djf.io): footer top border rendered near-white instead of zinc.800

The footer paired the `borderTop: '1px solid'` shorthand with the `borderColor`
shorthand. Panda emits each as its own atomic class, and `.bd-t_1px_solid` landed
after `.bd-c_zinc.800` in the stylesheet, so the shorthand reset `border-top-color`
back to `currentColor` -- the body's near-white `zinc.100` text color -- making the
bar above the copyright line glow bright against the dark theme. The header only
looked right because its `border-bottom` class happened to extract before the color
class. Switched both header and footer to the `borderBottomColor` / `borderTopColor`
longhands, which Panda deterministically sorts after shorthands, so the token color
always wins regardless of extraction order. Verified via computed styles in
Playwright: both borders now resolve to `rgb(39, 39, 42)`.

### feat(djf.io): remove AI attribution disclaimer from home page

Dropped the "AI Attribution & Disclaimer" section from the bottom of the home
page (`src/pages/index.astro`). The policy statement read awkwardly and no
longer reflects how the site is maintained.

### feat(djf.io): SEO meta, OG images, sitemap, JSON-LD; Lighthouse 100s

Brought djf.io to production SEO polish and closed out `docs/projects/djf-io-seo/`.
`BaseLayout` now emits a self-referencing canonical (normalized to the trailing-slash
URLs the directory build format publishes), Open Graph and Twitter card meta, RSS and
sitemap discovery links, and a named `head` slot for page-specific tags. Added
`@astrojs/sitemap` plus `public/robots.txt` pointing at `sitemap-index.xml`. Per-post
OG images (1200x630, dark zinc card matching the site) are generated at build time by
a new `src/pages/og/[...slug].png.ts` endpoint using satori + sharp, with Inter font
data resolved from `@fontsource/inter` so output is independent of build-host fonts;
non-post pages share a generated default card. `BlogPost` passes `og:type=article`
and its OG image through, and embeds BlogPosting JSON-LD (headline, datePublished,
author with fallback, url, image, keywords from tags). Fixed the one Lighthouse
accessibility finding -- footer copyright contrast -- by bumping `zinc.500` to
`zinc.400`. Local Lighthouse audits of the home page and a blog post now score
100/100/100/100 (performance/accessibility/best-practices/SEO). Tests: new
`src/seo.e2e.test.ts` crawls every sitemap URL asserting title, meta description,
canonical exactly equal to the sitemap `loc`, OG/Twitter meta, and a served og:image,
plus robots/sitemap/RSS/JSON-LD checks; unit tests cover the new meta and JSON-LD
fields (absolute-URL tags are e2e-only because the container API does not carry
`site` config through to `Astro.site`).

### fix(calendar-visualizer): correct holiday detection; first real tests

Writing the app's first unit tests surfaced two bugs in the calendar state
logic. (1) Holiday probing used local-midnight `Date` instants against
date-holidays ranges computed in US time, so on any machine at/east of UTC
(including CI runners) every holiday rendered one day late. (2)
`getHolidays(day)` returns the whole year's holidays, not that day's, so any
holiday-flagged day carried all ~11 holiday names. Fixed by fetching each
year's holidays once and matching by `YYYY-MM-DD` calendar date. The logic
moved from `calendar.tsx` into a new `calendar-state.ts` (no PandaCSS/React
imports) with 10 co-located vitest cases covering month slotting, spacer
padding, weekend/holiday tagging, and partial intervals — verified under UTC,
New York, Los Angeles, and Tokyo timezones. `passWithNoTests` removed now that
the suite exists.

### chore(tooling): wire Vitest in the three apps without test suites

calendar-visualizer, davidjfelix.com, and ravrun get `vitest.config.ts`, a
`test` script, a mise `test` task in the `check` umbrella, and a CI vitest job
— with `passWithNoTests: true` until the first real test lands. Astro apps
follow the djf.io `getViteConfig` pattern (plus a vitest/config type reference
since their tsconfigs typecheck config files); ravrun follows the f311x
standalone pattern with jsdom. All five apps now declare the full
typecheck/lint/format/test/build contract.

## 2026-06-09

### chore(tooling): per-app check tasks and full CI coverage (Phase 3b)

Every app under `apps/` now declares its canonical checks as mise tasks in an
app-level `mise.toml` (`typecheck`, `lint`, `format`, `test` where a suite
exists, `build`, plus a `check` umbrella). CI runs one job per check via
`mise run`, and the three apps that previously had no CI at all
(calendar-visualizer, davidjfelix.com, ravrun) got per-app workflows following
the existing paths-filter pattern. Supporting fixes: ravrun's `tsconfig.json`
gained `skipLibCheck` (its typecheck was failing on duplicate
`@tanstack/router-core` type declarations in `node_modules`),
calendar-visualizer gained `@astrojs/check` and had two unformatted files
fixed, app `format` scripts became check-mode (`format:fix` writes), and
Prettier was scoped to `**/*.{md,mdx}` globs because per-app
`prettier --write .` did not pick up the root `.prettierignore` and would have
rewritten Biome-owned files. Blog posts under `apps/djf.io/src/content/` are
excluded from Prettier via an app-level `.prettierignore` to preserve their
semantic-line-break authoring style.
