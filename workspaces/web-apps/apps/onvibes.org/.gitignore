node_modules
.DS_Store
dist
*.local
.env
.astro
.wrangler
.dev.vars

# Flue (cloudflare target) build + intermediate artifacts
dist-flue
.flue-vite
.flue-vite.wrangler.jsonc

# Panda codegen output (regenerated by the `prepare` script)
styled-system

# playwright
test-results/
playwright-report/
playwright/.cache/

# vitest coverage output
coverage/
