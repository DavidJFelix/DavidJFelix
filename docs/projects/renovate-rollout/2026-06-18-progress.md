# 2026-06-18 — kickoff (respawn)

Respawned from the closed `dependency-freshness` project. Renovate was given ownership of all
ecosystems on 2026-06-11; the freshness skill, cron, and slash command shipped and are now slated
for retirement once Renovate coverage is proven.

## Next

- Generalize `apps/djf.io/.github/renovate.json` to the whole repo (npm + mise + Cargo +
  `lockFileMaintenance`); confirm Dependabot is off.
- Pin the `latest`-tagged tsgo deps.
- Then retire the freshness skill + cron + `/dependency-freshness` command.
