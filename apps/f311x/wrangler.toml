name = "f311x"
compatibility_date = "2026-05-01"
compatibility_flags = ["nodejs_compat"]

# Deploy and bindings are managed by Alchemy v2 (see alchemy.run.ts); the app is
# built by Vite / TanStack Start, so there is no standalone worker entry here.
# This file is kept only as a fallback for `wrangler types` / `wrangler dev`.
