# 2026-05-13 Progress

## Summary

Planning session. Expanded the standard from JS-only to polyglot and replaced the formatter assumptions.

## Decisions

- cspell is universal — applies to every file type, every project
- JS/TS ecosystem: Biome (lint + format), Oxlint (additional rules), tsgo (type checking, part of checks suite)
- Drop Prettier. If Biome eventually supports MD/MDX, use it; until then, MD/MDX goes unformatted by automation
- Rust: clippy + rustfmt with shared config (aspirational — no Rust in repo yet)
- Go: golangci-lint + gofmt/goimports (aspirational — no Go in repo yet)
- Principle: same ecosystem → same config; per-project overrides only when justified
- Open to revisiting Biome later, but it's the right tool today

## Work Completed

- Rewrote `plan.md` with polyglot standard and explicit ecosystem-level ownership
- Added tsgo to the JS/TS checks suite
- Removed Prettier from the plan
- Removed dead link to the now-closed `dev-tooling-consolidation` project

## Next Steps

- Phase 1 audit of current lint/format/check configs across apps
- Sequence: complete dependency-freshness first (mise + skill); this project picks up after

## Open Questions

- Does tsgo land at v7-stable before we wire it in, or do we run a pre-release? (Defer until phase 2.)
- Do we want a `docs/tooling-standard.md` or just a CLAUDE.md section?
