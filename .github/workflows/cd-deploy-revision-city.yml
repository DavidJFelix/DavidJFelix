name: CD Deploy revision.city

on:
  push:
    branches: [main]
    paths:
      - 'apps/revision.city/**'
      - '.github/workflows/cd-deploy-revision-city.yml'

defaults:
  run:
    working-directory: apps/revision.city

jobs:
  deploy:
    name: wrangler deploy
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      deployments: write
    steps:
      - name: checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: mise
        uses: ./.github/actions/setup-mise

      - name: pnpm install
        run: pnpm install --frozen-lockfile

      # `pnpm install` already runs `panda codegen` via the prepare script, but
      # keep it explicit so the build does not depend on install side effects.
      - name: panda codegen
        run: pnpm exec panda codegen

      # Client observability vars, inlined at build by Vite. VITE_PUBLIC_SENTRY_DSN
      # turns the browser Sentry SDK on; VITE_PUBLIC_POSTHOG_KEY turns PostHog on.
      # Both public by design (repo variables). Unset means Vite dead-code-
      # eliminates the SDKs, so preview/CI ship zero vendor code; unset until issue
      # #261 sets them, so deploys stay dark. Worker-side Sentry reads SENTRY_DSN
      # at runtime from a Cloudflare Worker var (set separately, also #261).
      - name: vite build
        env:
          VITE_PUBLIC_SENTRY_DSN: ${{ vars.SENTRY_DSN_REVISION_CITY }}
          VITE_PUBLIC_POSTHOG_KEY: ${{ vars.POSTHOG_KEY_REVISION_CITY }}
        run: pnpm exec vite build

      # The Cloudflare Vite plugin emits the resolved Worker config into dist/
      # and a redirect in .wrangler/deploy/, so a plain `wrangler deploy` picks
      # up the built output.
      - name: wrangler deploy
        uses: cloudflare/wrangler-action@ebbaa1584979971c8614a24965b4405ff95890e0 # v4.0.0
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy
          workingDirectory: apps/revision.city
