# 2026-07-02 progress

## Session summary

Extended the project with scope item 3 (enforce code-style.md mechanically) and landed its first
tranche: four oxlint rules promoted to `error` in the root `.oxlintrc.json`, one at `warn` pending a
fix-up pass.

## Work completed

- Probed candidate oxlint rules empirically (deliberately-violating scratch files, then a sweep of
  every app + `bin/`) before enabling anything:
  - `max-params`, `typescript/consistent-type-imports`, `vitest/no-hooks`,
    `vitest/max-nested-describe` (max 0): zero violations everywhere → enabled at `error`
  - `typescript/consistent-type-definitions`: ~45 violations → enabled at `warn` (non-gating)
- Enabling the `vitest` plugin surfaced 13 pre-existing `correctness`-category findings; fixed them
  rather than silencing (per linting.md):
  - `vitest/require-mock-type-parameters`: `vi.fn(...)` → `vi.fn<typeof fetch>(...)` in the
    `sentry-tunnel.test.ts` copy of all 11 apps
  - `vitest/no-conditional-expect`: djf.io `content.config.test.ts` rewritten from `safeParse` +
    `if (result.success)` to `parse` + direct assertions
- Verified: `oxlint` exits 0 in every app; ravrun's sentry-tunnel suite (16 tests) passes and its
  typecheck shows no new findings. (djf.io's vitest could not run in the session sandbox —
  incomplete node_modules — so per-app CI is the authoritative check for that one.)
- Measured the scoped-out Rust burden for the record: ~48 clippy-pedantic findings across the
  Advent-of-Code 2022 workspace.

## Decisions made

- New always-fix rules get promoted to `error` individually once verified clean; no blanket
  `--deny-warnings`.
- Enforcement work lives here rather than a new project directory — same residual, same scope.

## Work completed (second pass, same day)

- Closed scope item 1: reflowed the 42 in-scope Markdown files, added the `format:md` /
  `format:md:fix` root mise tasks, and added `ci-docs.yml` to gate it. `.prettierignore` now
  excludes `apps/` (per-app gates own app markdown; djf.io's blog exclusion would otherwise be
  bypassed), `.agents/` (hash-locked by `skills-lock.json`), `.claude/`, and the legacy dirs.
- Aligned Prettier's embedded-code output with house style (`semi: false`, `singleQuote: true`,
  `bracketSpacing: false` in `.prettierrc.json`), which made the `prettier-ignore` guards around
  code-style.md's snippets unnecessary — removed.

## Next steps

- Fix the ~45 `type` → `interface` findings and promote `consistent-type-definitions` to `error`.
- Decide the fate of advisory (`warn`) categories: intended, or promote specific rules over time.

## Blockers / open questions

- None. Sandbox couldn't run djf.io's tests; CI covers it.
