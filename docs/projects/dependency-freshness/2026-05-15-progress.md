# 2026-05-15 Progress

## Summary

Flagged a real gap in the freshness sweep: it doesn't catch transitive dependency drift that Renovate is already PR-ing.

## Decisions

- Don't build a new tool for transitive freshness. Renovate's `lockFileMaintenance` is the canonical answer — `config:best-practices` (currently extended by `apps/djf.io/.github/renovate.json`) does not include it.
- Renovate owns transitive/lockfile freshness; the freshness skill owns manifest bumps + non-npm ecosystems (mise, Cargo) + cross-ecosystem batching.

## Work completed

- Added "Open: transitive dependency drift" section to `plan.md` with the division-of-labor decision and the open items.

## Next steps

- Enable `lockFileMaintenance` on djf.io's Renovate config and verify it produces lockfile PRs as expected.
- Extend Renovate coverage to the other apps (currently only djf.io has a Renovate config).
- Confirm Dependabot isn't double-driving updates on the same projects.

## Open questions

- None new — addressed via the plan update.
