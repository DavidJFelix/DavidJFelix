# 2026-07-23 — Diffs viewer ported from Pierre's diffshub

Ported the diffshub app from the PierreJS monorepo into `apps/revision.city` under the `/diffs`
prefix, per David's direction ("lift the diffshub functionality over to /diffs").

- Next.js -> TanStack Start: API routes became server routes (`/diffs/api/diff` streaming patch
  proxy, `/diffs/api/github-diff-file` context expansion); the catch-all viewer page became the
  `/diffs/$` splat route with canonicalizing redirects; `next/font` became Fontsource.
- Tailwind 4 -> Panda CSS rewrite across all components (David's call: keep the app on one styling
  system rather than adding Tailwind). Semantic tokens live under a `diffs` namespace; the CSS
  variable theme ships in `src/diffs/diffs.css`, loaded only on /diffs routes.
- Branding neutralized (David's call): no DiffsHub/Pierre marks, socials, or team avatar photos;
  JetBrains Mono (Fontsource) replaces the commercial Berkeley Mono; comment personas are neutral
  initial-letter avatars; the pierrecdn demo-blob cache was dropped.
- `@pierre/{diffs,trees,theme,theming,icons}` consumed as published npm packages.
- Tests ported to flat co-located vitest; smoke gate now probes `/diffs`; landing page Diffs card
  links to the viewer (David's call).

The reviews MVP-shape question (see plan.md) remains open and gated on David; this work is the
diffs half of the "Reviews + diffs" positioning.
