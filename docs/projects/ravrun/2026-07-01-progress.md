# ravrun — 2026-07-01 progress

<!-- cSpell:words VEVENT DTSTAMP DTSTART -->

## Session summary

Built the training-plan engine test-first (TDD, red-green per behavior) in
`apps/ravrun/src/lib/plan/`: pace math, a phased progressive-mileage generator anchored on the race
date, and a feasibility checker. David's real 2026 marathon plan (yearroundrunning.com ICS export)
served as the reference for the week shape and constants.

## Work completed

- `constants.ts` — every tunable training rule in one central object (David's explicit ask): Riegel
  exponent, pace offsets/band widths, week template rotation, stepback cadence (every 4th week,
  0.8×), peak long runs (marathon 20mi), taper factors, long-run share of weekly volume (0.42), ramp
  cap (10%), goal-gap thresholds (5%/10%), minimum durations.
- `paces.ts` — Riegel race-time prediction, training pace bands (easy/long/recovery/tempo/interval)
  derived from a recent race, duration/pace parse + format helpers.
- `generate.ts` — race-date-anchored plan: N weeks ending on race day; base → build → peak → taper →
  race phases; week template rotates around the chosen long-run day (matching David's lived plan:
  long Sat, recovery Sun, rest Mon, tempo Tue, quality Thu); intervals appear from build onward;
  long-run progression from current fitness to the distance peak with stepback dips and taper
  wind-down; weekly volume derived from the long run; race week keeps easy volume + shakeout.
- `feasibility.ts` — findings with levels: `aggressive-goal` / `unrealistic-goal` (goal vs. Riegel
  prediction), `ramp-too-steep` (with a `suggestedWeeks` that provably resolves its own warning),
  `plan-too-short`, `window-underway` / `race-passed`.
- 20 unit tests, flat (no `describe`), co-located; repo coverage gate (100% statements on
  `src/lib/**`) green; full `mise run check` green; cspell taught riegel/stepback(s)/shakeout.
- Ignored vitest's `coverage/` output dir in the app `.gitignore` (stray temp files were failing
  biome).
- Docs corrected: domains are **live** (David confirmed; dashboard-configured, not in
  `wrangler.toml`).

## Decisions

- **Methodology**: tried-and-true progressive mileage (Higdon-shaped) + Riegel/Daniels-lite paces,
  not a bespoke model. Encoded as rules, not literal plan tables, so duration and schedule shape
  stay flexible.
- **Race date is the anchor**; duration is chosen (default 18wk marathon). The "how long do I
  actually need" solver arrives later as the feasibility engine inverted (`suggestedWeeks` seeds
  it).
- **Stepback every 4th week at 0.8×** (David's lived plan), not the every-3rd/75% first proposed.
- **Peak long run 20mi / 2-week marathon taper** as defaults (David's ICS runs 22mi / 1-week — a
  one-line constants change if he prefers).
- All constants centralized; changing one should consciously break an anchor test.
- Post-race continuation is out of scope for now; the phase model leaves room for appended blocks.

## Session 2 (same day): UI, ics export, pace drift

David said "do it all" — the whole next-steps list landed in a second push:

- `ics.ts` (TDD): deterministic `.ics` export — one all-day VEVENT per workout with distance, pace,
  and week/phase context; DTSTAMP derived from the plan start so exports are reproducible.
- Pace-band drift (TDD): when the goal outruns predicted fitness, bands interpolate linearly from
  current-fitness paces to goal-fitness paces across the block; a slower goal never drags paces
  backward (the pre-existing static-band test pins that rule).
- Index route rewritten: form (race, date, goal, weekly mileage, recent race, plan length, long-run
  day) with config in URL search params; color-coded grid (workout chips, phase + stepback labels,
  weekly totals); feasibility banners with one-click "Use N weeks"; Download `.ics` button. The
  hardcoded demo generator is gone.
- Visual e2e baseline re-recorded against a fully-pinned plan URL (a `today` search param keeps
  feasibility deterministic under test). Full `mise run check`, Playwright e2e, and smoke all green;
  25 lib tests, 100% statement coverage maintained.
- cspell taught the iCalendar vocabulary (vevent, dtstart, ...) and `lerp`.

## Session 3 (same day): low-hanging fruit sweep

- `conservative-goal` feasibility finding (TDD): a goal >5% slower than predicted gets an info nudge
  — "this goal has headroom, you could aim faster."
- `/about` rewritten from a stub into a real explainer (philosophy, how plans build, paces, honesty
  checks, sharing/export).
- Polish: real `<title>` + meta description (was "TanStack Router"), ravrun brand in the nav,
  workout-type color legend, today's date ringed in the grid (driven by the same `today` param the
  e2e pins), Copy-link button next to Download `.ics`, horizontal scroll for the grid on small
  screens.
- Baseline re-recorded; check, e2e, smoke, spell all green.

## Next steps

- Taste-tune constants against rendered plans (peak long run 20 vs 22, marathon taper 2 vs 1).
- Days-per-week flexibility; post-race continuation blocks.
- Deploy and eyeball on ravrun.com.

## Blockers / open questions

- None blocking. Open taste questions for David: peak long run 20 vs 22, marathon taper 2 weeks vs 1
  (both one-line constants edits).
