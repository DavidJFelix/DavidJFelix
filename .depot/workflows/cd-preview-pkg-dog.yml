# Depot CI Migration
# Source: .github/workflows/cd-preview-pkg-dog.yml
#
# Changes made:
# - Rewrote .github/ path references to .depot/
# - Changed GitHub runs-on labels to their Depot equivalents

name: CD Preview pkg.dog
# Per-PR preview deploy for pkg.dog (wrangler, Nuxt SSR). On every PR touching
# the app: upload a non-active version with a pr-<N> preview alias, smoke the
# preview URL, screenshot-diff it against committed baselines, and post the URL.
# No teardown -- `wrangler versions upload` never serves production, so the
# custom domains stay on the active version. Forks have no secrets and will fail
# the deploy step -- acceptable for a personal monorepo; do NOT switch to
# pull_request_target.
on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'apps/pkg.dog/**'
      - '.depot/workflows/cd-preview-pkg-dog.yml'
      - '.depot/actions/preview-wrangler/**'
      - '.depot/actions/snapshot-setup/**'
      - 'bin/**'
concurrency:
  group: preview-pkg-dog-${{ github.event.pull_request.number }}
  cancel-in-progress: true
defaults:
  run:
    working-directory: apps/pkg.dog
env:
  MISE_TRUSTED_CONFIG_PATHS: ${{ github.workspace }}
jobs:
  snapshot:
    name: snapshot image
    # Shared pkg-dog sandbox image (mise tools + node_modules + Chromium).
    # Depot skips this job while the image tag exists; the other jobs boot
    # from the image. See docs/contributing/github-actions-style.md.
    runs-on: depot-ubuntu-latest
    snapshot:
      image-name: pkg-dog
      version: v1
      with:
        max-age: 7d
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - name: checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: snapshot setup
        uses: ./.depot/actions/snapshot-setup
        with:
          app: pkg.dog
  preview:
    name: deploy + smoke + screenshots
    needs: snapshot
    runs-on:
      image: pkg-dog
      size: 2x8
    timeout-minutes: 20
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
          clean: false
      - name: mise install
        run: mise install
      - name: preview
        uses: ./.depot/actions/preview-wrangler
        with:
          worker-name: pkg-dog
          pr-number: ${{ github.event.pull_request.number }}
          smoke-routes: /
          cloudflare-api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          cloudflare-account-id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
