# Canonical check tasks for this app. CI and automation discover checks here;
# tasks delegate to package.json scripts. Assumes `pnpm install` has run.

# bun runs the deploy/smoke scripts in bin/ (repo rule: scripts are bun, not
# bash); the app itself stays on the Node/pnpm toolchain.
[tools]
bun = "1"

[tasks.typecheck]
description = "Type-check with tsgo"
run = "pnpm run typecheck"

[tasks.lint]
description = "Lint with Biome + Oxlint"
run = "pnpm run lint"

[tasks.format]
description = "Check formatting (Biome; Prettier for md/mdx)"
run = "pnpm run format"

[tasks."format:fix"]
description = "Apply formatting fixes"
run = "pnpm run format:fix"

[tasks.test]
description = "Unit tests with Vitest"
run = "pnpm run test"

[tasks.build]
description = "Build with Vite"
run = "pnpm run build"

[tasks.check]
description = "Run all checks"
depends = ["typecheck", "lint", "format", "test", "build"]
