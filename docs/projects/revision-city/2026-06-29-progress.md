# revision.city — 2026-06-29 progress

## Session summary

Portfolio status review. Revisited the one blocking decision for revision.city — the MVP shape — and
deferred it, recording it explicitly in `plan.md` as an open question David will answer himself.

## Work completed

- Sharpened `plan.md` to make clear the MVP shape is _not_ yet captured: added an "Open question —
  MVP shape" callout in Vision, and reframed Phase 1 ("Position the concept") and Phase 2 ("David
  defines the MVP shape") so the plan no longer proposes or pins a direction.

## Decisions

- **MVP shape deferred.** It is a nuanced vision David will articulate himself, in his own words; it
  does not reduce to a multiple-choice pick, so the plan deliberately does not suggest one. The
  earlier "hosted VCS vs. review layer vs. diff viewer" framing was removed as the answer.
- A design/positioning doc — written by David — is the gate before any build.

## Next steps

- Phase 1 stays "position the concept": layout (done) + a positioning line.
- David writes up the MVP shape when ready; that doc unblocks Phase 3.

## Blockers / open questions

- **MVP shape is the open, blocking question.** David will define it himself; nothing downstream is
  scoped until he does.
