# forzamonica.com — 2026-06-19 progress

## Session summary

Renamed the project directory `forzamonica-shop` → `forzamonica-com` and reframed it as the
forzamonica.com app umbrella, part of the repo-wide "project per app" portfolio revision. No change
to the app or its blockers.

## Work completed

- `git mv docs/projects/forzamonica-shop docs/projects/forzamonica-com` (pure rename).
- Retitled the plan to `forzamonica.com (Forza Monica Shop)` to match the per-app umbrella naming.

## Decisions

- The directory name now follows the domain-kebab convention used by the other app umbrellas (and
  the `cd-deploy-forzamonica-com.yml` workflow).

## Next steps

- Unchanged: clear the two human blockers (Shopify store + token; register the domain) — see the
  filed issues and the plan's Phase 2.

## Blockers / open questions

- Unchanged from the plan: production is blocked on the two human tasks.
