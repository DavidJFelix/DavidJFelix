# 2026-04-30 — davidjfelix.com scaffolded

## Done

- Scaffolded `apps/davidjfelix.com/` with Astro 6.2.1 (latest)
  - Minimal single-page site (`src/pages/index.astro`) with inline dark theme styles, no React/PandaCSS yet
  - Strict TS config extending `astro/tsconfigs/strict`
  - Biome config extends root `biome.jsonc`
- `wrangler.toml` for Cloudflare Workers Static Assets (no custom domain route — pending DNS cutover)
- `.github/workflows/davidjfelix-com-deploy.yml` mirroring the calendar-visualizer / djf.io pattern (push to main, path-filtered)
- Verified `pnpm install` and `pnpm build` succeed locally

## Next

- Merge to main, confirm Cloudflare Workers deploy succeeds
- After djf.io DNS cutover playbook is proven, repeat for davidjfelix.com:
  - Add `routes` with `custom_domain = true` to `wrangler.toml`, or attach via Cloudflare dashboard
- Decide on real content for davidjfelix.com (resume vs landing) before further build-out
- Pick the next domain to scaffold (likely `monicandavid.com` since it's also Astro)
