# 2026-09-21 progress

## Session summary

David answered the open decisions (runtime rendering, one PR, design-token colors, shared template,
ravrun.com, no new twitter handles); the whole rollout was then implemented on the branch.

## Work completed

- `@davidjfelix/og`: `ogSite`, the renderer rebuilt on satori's standalone build plus
  `@resvg/resvg-wasm` with `OgTheme`, `ogCard` with Workers Cache API caching, the Vite, Nitro, and
  Node runtimes with consumer type surfaces, `pngSize`, a Vitest bundler stand-in so the runtime
  modules are tested as written; `sharp` dropped; relative imports made extension-less so
  svelte-check accepts the source.
- satori held at 0.32 (Renovate rule): 0.33's HarfBuzz shaper crashes on Workers.
- djf.io moved to the wasm rasterizer with its prerendered cards intact (unit, build, seo e2e all
  green).
- Cards, tags, canonical links, and e2e cases across startchi.com, davidjfelix.com,
  monicandavid.com, ravrun, revision.city, forzamonica.com, pkg.dog; tags only on f311x and
  calendar-visualizer; e2e suites exercise the real workerd path in every case.
- Spelling: `wasm` topic dictionary (Emscripten, HarfBuzz); npm names (resvg, unwasm, harfbuzzjs).

## Decisions made

See "Decisions" in [plan.md](./plan.md). Two made while implementing: SvelteKit loads the runtime on
the first request because its build imports route modules in Node; f311x gets tags only because its
alchemy pipeline has no wasm module rule.

## Next steps

1. Review and merge the PR.
2. On the first production deploy, fetch one `/og/default.png` cold and confirm the Workers plan's
   CPU budget covers it (see "Risks" in plan.md).
3. Close the project into the changelog; keep the follow-ups list as new efforts.

## Blockers or open questions

- The Workers plan tier is not knowable from the repo; the free plan's 10 ms CPU cap would fail a
  cold render.
