# 2026-06-11 Progress

## Summary

Current-state audit session, requested by David to test whether the remaining
work is really the "small finishing work" the 2026-06-09 note implied.
Verdict: it is not. The remaining scope is 2–4 sessions, and the largest plan
item was never started. The plan now carries a "Remaining work (2026-06-11
audit)" section; this note records the findings.

## Findings

1. **Rust ecosystem at 0%.** The plan specifies shared clippy (extensive
   ruleset) + rustfmt configs and workspace lint config. None exist — no
   `clippy.toml`, no `rustfmt.toml`, no lint section in root `Cargo.toml`, no
   Rust CI at all. Coverage gap spans 22 crates: 14 Advent-of-Code 2022
   workspace members and 8 standalone Exercism crates.
2. **cspell configured but enforced nowhere.** `.config/cspell.json` and
   per-app `pnpm run spell` scripts exist, but no mise `check` umbrella
   includes spell, there is no root spell task, and none of the 16 CI
   workflows run cspell.
3. **Root `.prettierignore` non-functional.** `*` + `!**/*.md` cannot
   re-include nested files (gitignore semantics); needs the `/*` + `!*/`
   recipe. README/CLAUDE/AGENTS/CONTRIBUTING and ~43 files under `docs/` are
   not prettier-clean.
4. **Unscoped directories.** `Joy-of-React/` has two projects with Biome
   configs extending root but no CI; `Advent-of-Code/2020/*/typescript` has
   eight projects with no lint/format configs at all. The standard silently
   covers only `apps/`.
5. **Config drift.** davidjfelix.com and djf.io on Biome schema 2.4.4 vs
   2.4.16 elsewhere.
6. **Phase 4 missing.** No `docs/tooling-standard.md`; CLAUDE.md tooling
   section lacks Rust and cspell entries.
7. **Correction to the audit's tsgo flag**: tsgo is a proper per-app
   devDependency (`@typescript/native-preview`), so no install gap — but it is
   pinned to `latest` in four apps; handed to dependency-freshness to pin when
   Renovate coverage extends.

## Decisions made

- None on scope yet. Three decisions are pending with David (recorded in the
  plan): Rust depth (full / minimal / scope-out — recommendation: minimal or
  scope-out for exercise code), legacy JS dirs (wire in / scope out /
  archive), and root markdown (format everything vs exclude docs from prose
  rewrapping).

## Next steps

- Get the three scope decisions from David.
- Independently of those: the mechanical slice (cspell wiring into mise
  `check` + a CI job, `.prettierignore` fix, Biome schema bumps,
  `docs/tooling-standard.md`) — roughly one session.

## Blockers or open questions

- The three scope decisions above block the Rust, legacy-dir, and markdown
  items.
