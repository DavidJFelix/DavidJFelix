# Claude Code

[AGENTS.md](AGENTS.md) is the canonical agent entry point for this repository; it is imported below,
and everything it links to applies to Claude Code sessions. Claude-specific surfaces: skills in
`.agents/skills/` and review personas in `.agents/agents/` (both reached through tracked symlinks
under `.claude/`), hooks in `.claude/hooks/`.

@AGENTS.md
