# 2026-05-15 Progress

## Summary

Added scope for per-project subtree checks to the standardization plan.

## Decisions

- Per-project linters, format checks, tests, and builds should run when a project's subtree changes — not just a single repo-wide suite.
- Each app declares its check commands in a discoverable form (mise tasks preferred) so contributors (human and AI) don't have to memorize per-app conventions.
- Repo-wide checks (cspell, root Biome/Oxlint) continue to run on every PR.

## Work completed

- Added Phase 3b ("Per-project subtree checks") to `plan.md`.

## Next steps

- Inventory which apps already declare lint/format/test/build commands and in what form.
- Pick the change-detection mechanism for CI (paths filter vs. turbo-style affected detection — keep this simple, no new task tooling without an ask).

## Open questions

- None.
