# 2026-06-18 — kickoff (respawn)

Respawned from the closed `linter-formatter-standardization` project. The JS/TS standard is set and
documented; the three open scope decisions were resolved at close (Rust scoped out; legacy JS dirs
folded in at low priority; all of `docs/` to be formatted + guarded). This project carries only the
concrete residual cleanups.

## Next

- Format `docs/` with Prettier and add the `prettier --check` guard (decision #3).
- Rename `.config/cspell.json` → `.jsonc` — after `ci-pipeline-efficiency` removes it from the
  per-app CI filters.
- Fold the legacy JS dirs in (low priority).
