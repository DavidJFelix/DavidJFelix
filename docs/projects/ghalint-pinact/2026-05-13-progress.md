# 2026-05-13 Progress

## Summary

Expanded project scope from `ghalint + pinact` to `actionlint + ghalint + zizmor + pinact`. The install/run/CI-enforce shape is the same for all four, and they have non-overlapping coverage of correctness, policy, security, and SHA-pinning.

## Work Completed

- Rewrote `plan.md` to cover all four tools across the same three phases.
- Updated `docs/projects.md` entry title to "GitHub Actions Hygiene" and refreshed the description.

## Decisions

- Kept the project directory name as `ghalint-pinact` to avoid churn on the existing path. The project name in `projects.md` is the canonical label.
- Scoped out broader PR-security tooling (`dependency-review-action`, `gitleaks`, OpenSSF Scorecard, CodeQL) — deferred to a separate future project.

## Next Steps

- Phase 1: add the four tools to `.config/mise.toml` and confirm each runs locally.
- Phase 2: fix existing workflow findings (most workflows are missing top-level `permissions:` and use floating major tags).
- Phase 3: add `.github/workflows/actions-lint.yml` to enforce in CI.
