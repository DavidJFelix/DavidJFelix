# 2026-06-04 progress

## Session summary

Reviewed PR #204 against the preference for avoiding `useEffect` where possible
and avoiding lint ignores or disabled rules.

## Work completed

- Removed the handwritten `useEffect` and `biome-ignore` from the f311x code block
  component by rendering code blocks as plain React instead of async Shiki HTML.
- Removed unused `@cloudflare/ai-chat` and `shiki` dependencies.
- Switched production deploys to run after a successful `CI f311x` workflow on
  `main` instead of racing CI on the same push.
- Added the CD workflow file to the f311x CI path filters so deploy workflow edits
  are checked before they can deploy.
- Stabilized Markdown block keys by token offset so streaming content updates do not
  remount existing blocks.

## Verification

- `pnpm dlx @biomejs/biome@2 check .`
- `pnpm dlx oxlint@1` (exited successfully with existing warnings)
- `pnpm run typecheck`
- `pnpm run build`
- `pnpm run test`

## Decisions made

- Left the generated `src/routeTree.gen.ts` suppressions alone. They are emitted
  by TanStack Router, excluded from Biome, and marked read-only in editor config.
- Kept PR CI credential-less. Alchemy's Cloudflare deploy build still runs only in
  CD after merge because exercising it requires Cloudflare credentials.

## Next steps

- Watch the first `main` deploy after merge to confirm the CI-gated CD handoff and
  Cloudflare permissions.

## Blockers or open questions

- None.
