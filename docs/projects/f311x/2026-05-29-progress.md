# 2026-05-29 Progress

## Summary

Recap session. Reframed f311x status: the scaffold builds, but the app is not
functional and deployment is still manual. Added two phases to the plan.

## Decisions

- f311x is **not** near done — most functionality is missing (stubs / unverified
  surfaces). Scaffold milestone ≠ working agent.
- Deployment must move to **continuous delivery** (a `cd_deploy_f311x.yml`
  pipeline), not a manual `pnpm deploy` from a local machine.

## Work completed

- Added a "Status (2026-05-29)" section to `plan.md`.
- Added **Phase 9 — Make it actually work (functionality)**: Alchemy CLI contract,
  binding wiring, Vectorize provisioning, dynamic-workflow loader, sandbox path,
  AI Gateway routing, chat transport, and live tool verification.
- Added **Phase 10 — Continuous delivery**: path-filtered CD workflow per the
  repo style guide, Alchemy/wrangler deploy on push, Cloudflare auth via secrets,
  out-of-band resource provisioning, first automated deploy as the done-bar.

## Next steps

- Pick the next functionality target (likely: confirm Alchemy v2 CLI entry
  contract, since it unblocks binding wiring).
- Stand up `cd_deploy_f311x.yml` following the existing `cd_deploy_*` workflows.
- File human-intervention issues for any Cloudflare account / secret setup the
  automated deploy needs.

## Open questions

- Deploy tool for CD day one: Alchemy v2 CLI vs `wrangler deploy`? Gated on
  confirming the v2 CLI entry contract.
- Which Cloudflare resources must exist before the first automated deploy can
  succeed (Vectorize index, AI Gateway, secrets)?
