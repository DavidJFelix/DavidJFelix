// Browser-mode test setup: pull in the app stylesheet so the Panda-generated
// CSS (layout, breakpoints, the drawer transform) is live in the test page,
// the same way __root.tsx links it in the real app.
import './styles.css'
