# 2026-06-12 — five placeholder sites scaffolded

## Done

- Scaffolded the five remaining domains as minimal landing pages (title only),
  each a standalone pnpm project with the repo-standard toolchain (Biome +
  Oxlint, Prettier for md/mdx, Vitest with `passWithNoTests`, mise check tasks,
  per-app CI workflow):
  - `apps/onvibes.org` — Astro + PandaCSS, `astro check` for types
  - `apps/monicandavid.com` — SvelteKit + adapter-cloudflare + PandaCSS + Ark UI
    (switched from Astro per David, 2026-06-12)
  - `apps/pkg.dog` — Vue 3 (Vite SPA) + PandaCSS + Ark UI
  - `apps/revision.city` — TanStack Start + PandaCSS + Ark UI (forzamonica
    pattern, port 3005)
  - `apps/startchi.com` — TanStack Start + PandaCSS + Ark UI (port 3006)
- PandaCSS everywhere with the default `@pandacss/preset-panda`, codegen via the
  `prepare` script, `styled-system/` gitignored
- Verified locally with the CI toolchain (mise: node 26, pnpm 11): build,
  typecheck, lint, format, and test all pass for all five apps

## Decisions

- Type-check with tsgo (`@typescript/native-preview`) across the board per
  David ("give me tsgo 7") — `typescript`, `svelte-check`, and `vue-tsc` are
  not installed. Trade-off: Svelte/Vue template type-checking is skipped until
  those tools support TS 7 (`pkg.dog/src/vue-shims.d.ts` documents this).
- Ark UI installed in all non-Astro apps to lock in the component library
  choice; no components used yet on title-only pages.
- Dev ports: 3003 monicandavid.com, 3004 pkg.dog, 3005 revision.city,
  3006 startchi.com (3000-3002 already taken by f311x/ravrun/forzamonica).

## Next

- CD workflows (workers.dev deploys) once David wants these live; routes blocks
  are pre-written as comments in each `wrangler.toml`
- Custom-domain wiring per site when zones are connected
- Real content, starting wherever David directs
