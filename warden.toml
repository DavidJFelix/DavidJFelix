# Warden — AI code review on pull requests.
# Guide: https://warden.sentry.dev/guide  ·  Source: https://github.com/getsentry/warden
#
# Runtime is Pi pointed at OpenRouter, so the review model is swappable without
# editing this file: the model is chosen via the WARDEN_MODEL env/secret
# (an "openrouter/<provider>/<model>" selector), and WARDEN_OPENROUTER_API_KEY
# supplies the credential. CI reads both from Depot secrets (the workflows run
# on Depot CI, which never sees GitHub repo secrets). For a local `warden run`,
# export the same two vars. The model must exist in the Pi runtime's bundled
# catalog, not just on OpenRouter — an uncataloged slug fails every chunk with
# a misleading "usually an authentication problem" error.
#
# Lives at the repo root because both the CLI and the getsentry/warden action
# resolve `./warden.toml` relative to the git root (a documented-default, like
# Cargo.toml — see docs/contributing/configuration-style.md). Skills are managed with
# `warden add <name>` / `warden sync`. The bundled `warden` / `warden-sweep`
# agent-authoring skills that `warden init` offers were deliberately not
# installed into .agents/skills/ — add them later only if we author custom
# review skills.

version = 1

# Inherited by every skill below.
[defaults]
runtime = "pi"
# Severity gate. failOn: lowest severity that fails the PR check.
# reportOn: lowest severity posted as an inline annotation.
# Levels: critical, high, medium, low, info, off.
failOn = "high"
reportOn = "medium"
# Without this, findings at/above failOn post as a neutral check, never a
# failure — Warden's determineConclusion only returns "failure" when failCheck
# is set. This is what makes the gate actually gate.
failCheck = true
# No reportOnSuccess: in 0.48.0 a clean run renders a body-only COMMENT review
# and the poster drops those before calling GitHub, so the option is inert. The
# per-run summary comment on the PR comes from bin/comment-warden-summary.ts,
# run as the last step of .depot/workflows/ci-warden.yml.

# Reasoning effort for the repo-aware review agent (off, low, medium, high,
# xhigh, max; runtime default when omitted). This is the wall-time dial as
# much as the cost one: at high, single hunks of a 27-file PR ran for up to
# eighteen minutes on 2026-09-24 and a whole run for twenty, most of it the
# model reasoning at length on every turn, while the four findings the same
# diff drew were all plain to a medium read. Turn it back up if a review
# starts missing things, not if it merely gets quicker.
[defaults.agent]
effort = "medium"
# Round trips one hunk may take before its review is cut short (the runtime
# allows 50). The eighteen-minute hunks were exploration: a review that has
# not settled after this many turns is chasing the repo, not the diff.
maxTurns = 25

# Hunk analyses in flight at once, across both skills; the action's `parallel`
# input (5) is the fallback when this is unset. Five wide, a 27-file PR at
# high effort overran ci-warden.yml's 20-minute cap twice on 2026-09-24, and
# twenty wide it fit by seconds. The ceiling is OpenRouter's credit balance,
# not the model: every in-flight request reserves its worst-case cost up
# front, and a request the balance cannot cover is refused ("would exceed
# your available credits given your current in-flight requests"), which
# Warden counts as a provider failure and stops the run after five in a row.
# Twenty ran once and was refused the next time, so this stays at what the
# balance carries with room to spare; top the balance up before raising it.
# Spend is per token, so the width moves wall time, not cost.
[runner]
concurrency = 8

# Built-in baseline application-security review. Whole-repo: no `paths` filter,
# so it sees the entire PR diff across all apps and root config.
[[skills]]
name = "security-review"

# Always-on for non-draft PRs. With `draft` set, the matcher runs on
# (non-draft) OR (has this label), and `labeled` events only match this
# specific label. Net effect: every ready PR reviews on open and on each push,
# random labels never trigger re-reviews, and adding the "Warden" label is the
# manual lever — it re-runs a review on the current head (remove and re-add
# to re-run again) and opts a draft in. .depot/workflows/ci-warden.yml carries
# the same filter at the job level so skipped events never allocate a runner,
# plus one rule this file cannot express (triggers filter on action, draft,
# label, and paths — not author): PRs opened by the repo's GitHub App
# (Renovate bumps, the changelog roll-up) skip the automatic review and only
# run when labeled.
[[skills.triggers]]
type = "pull_request"
actions = ["opened", "synchronize", "reopened", "ready_for_review", "labeled"]
draft = false
labels = ["Warden"]

# Built-in correctness / logic-bug review. Also whole-repo.
[[skills]]
name = "code-review"

[[skills.triggers]]
type = "pull_request"
actions = ["opened", "synchronize", "reopened", "ready_for_review", "labeled"]
draft = false
labels = ["Warden"]
