# startchi.com — 2026-06-29 progress

## Session summary

Status-review session confirming the MVP and making the roadmap concrete in `plan.md`.

## Work completed

- Confirmed the MVP: the **directory** (submit + browse startups) is the first real build.
- Made Phase 2 concrete: a submit-a-startup flow plus a browse/list view of submitted startups.
- Marked the four-part vision (directory, signal boost, org hub, identity) so the directory is the
  confirmed MVP and signal boost / org hub / identity are explicitly later layers.

## Decisions

- The directory is the confirmed first real build; community / signal-boost / org-hub features layer
  on top of it.

## Next steps

- Phase 1: finish basic layout + positioning.
- Phase 2: build the directory MVP — submit flow + browse/list view.

## Blockers / open questions

- None blocking the layout work; the directory build needs a data model + storage decision when it
  starts.
