# 2026-07

## 2026-07-05

### chore(repo): isolate nested workspace trees

Moved the Daily UI, Joy of React, and Advent of Code code into `workspaces/` so workspace roots can
stay contained away from the parent repo. The moved trees were renamed to kebab-case, including
local React component files, generated artifacts were left out of the new locations, and the root
Cargo workspace now reserves `crates/*` for first-party Rust crates while continuing to include the
Advent of Code 2022 Rust crates.

### chore(warden): gate the CI review behind the Warden label

Warden reviewed every non-draft PR on every push, and the per-push model spend added up fast. The
review is now on demand: `ci-warden.yml` triggers only on `labeled` events with a job-level guard on
the `Warden` label, so other labels never allocate a runner and nothing runs (or bills) unless
asked. Adding the `Warden` label to a PR -- drafts included -- reviews its current head; remove and
re-add the label to re-run. The `getsentry/warden` action cannot review a PR from
`workflow_dispatch` (it routes dispatch events to schedule-type sweep triggers, and its
analyze/report modes require a pull_request event), so the label is the dispatch button for PR
reviews; a second job in the same workflow covers true `workflow_dispatch` by running the
mise-pinned warden CLI against whatever branch the dispatch selects (diffed from its merge-base with
a `base` input, default `main`), with findings in the job log rather than posted to a PR.
`warden.toml`'s triggers now mirror the label filter (`actions = ["labeled"]`,
`labels = ["Warden"]`), and CONTRIBUTING plus the review-consolidation plan record the always-on to
label-gated decision.

## 2026-07-03

### feat(djf.io): serve pages as markdown to agents that ask

`Accept: text/markdown` now returns any page as markdown (`Content-Type: text/markdown` plus an
`x-markdown-tokens` estimate) while browsers keep getting HTML -- the contract of Cloudflare's
Markdown for Agents, implemented in the worker so it stays plan-independent and testable. A new
worker entry (`src/worker.ts`) wraps the `@astrojs/cloudflare` handler with `negotiateMarkdown`
(`src/lib/markdown-for-agents.ts`), which converts successful HTML GETs: YAML frontmatter from the
title and meta description, then the `<main>` content as GitHub-flavored markdown with the site
chrome stripped. Negotiation reads a request header, so wrangler's `run_worker_first` now routes
page paths through the worker; hashed-asset prefixes keep bypassing it. Unit tests cover the
negotiation and conversion logic under the existing 100% lib coverage gate, and e2e tests prove the
negotiation against the built worker in workerd.

### fix(repo): make the Warden gate enforce failures and tidy the install

Follow-ups to the Warden install (#306), which merged with the gate live but not yet enforcing.

- `warden.toml`: set `failCheck = true` — without it Warden's `determineConclusion` posts findings
  at/above `failOn` as a **neutral** check, never a failure, so `failOn = "high"` was inert and the
  "gate" could not block a PR. Also filtered the `labeled` trigger to `labels = ["Warden"]`: a
  random label no longer re-runs a full model-backed review; adding the `Warden` label is now the
  manual re-run lever (and opts in a draft PR).
- `ci-warden.yml`: dropped the job-level `GITHUB_TOKEN` env — the warden action's `github-token`
  input already defaults to the workflow token, so the env copy only added exposure (every child
  process of the model-backed analyze step could read it). Rewrote the analyze/report split comment
  honestly: with both steps on the workflow token the split is a findings handoff, not a credential
  boundary. Header now says the secrets come from Depot (the workflows run on Depot CI, which never
  sees GitHub repo secrets).
- `.github/renovate.json`: pointed the `github-actions` manager at `.depot/workflows/` via
  `managerFilePatterns` — Renovate's defaults only match `.github|.gitea|.forgejo` workflow paths,
  so every action pin in the Depot workflows (including `getsentry/warden`) was invisible to update
  automation.
- Documented the deliberate skip of Warden's bundled `warden`/`warden-sweep` authoring skills and
  the Pi-runtime model-catalog constraint in `warden.toml`; added local-run instructions to
  CONTRIBUTING's Code review section; updated the review-consolidation project (Phase 1 complete).

Also the durable record of the late fixes that shipped inside #306 after its 2026-06-30 changelog
entry (the project's progress notes are ephemeral):

- **Node 22+ runtime for the Warden action.** `getsentry/warden` is a composite action running bare
  `node dist/action/index.js`; on the runner's default Node 20 its bundled undici (>=8.0.3) throws
  `TypeError: webidl.util.markAsUncloneable is not a function` (API landed in Node 22.10,
  nodejs/undici#5024). Fixed by adding the `setup-mise` step (repo-pinned Node 26) to the workflow.
- **`ready_for_review` trigger** added to both the workflow and both skills' triggers (CodeRabbit
  finding): a PR opened as draft and later marked ready previously fired no covered event.
- **Repo-wide biome 2.5.0 schema migration.** Main's lockfile maintenance had bumped the biome CLI
  to 2.5.0 while every `biome.json[c]` still declared the 2.4.16 schema — latent breakage that
  path-filtered CI had not surfaced. `biome migrate --write` across root + 11 apps + 2 legacy dirs.
- **cspell words alphabetized** while resolving rebase conflicts; merged with main's additions.
- **Secrets clarified as Depot secrets**, not GitHub repo secrets (issue #302, closed once the gate
  authenticated). Diagnosed en route: a `WARDEN_MODEL` slug must exist in the Pi runtime's bundled
  model catalog, not just on OpenRouter — an uncataloged slug fails every chunk with a misleading
  "usually an authentication problem" error.

### feat(onvibes-org): add a Flue agent chat interface

Added a real-time chat page (`/chat`): a React island streaming messages from an `assistant` agent
over HTTP. Flue now owns the deployed Cloudflare Worker and hosts Astro inside it — `src/app.ts`
(Hono) mounts the agent API at `/api` and forwards everything else to the prebuilt Astro worker. The
agent is a keyless faux echo (swap point for a real model documented in `src/agents/assistant.ts`),
backed by Durable Objects with SQLite storage. The build gained a worker phase (`flue build` plus an
assets-binding patch step), smoke now boots the real Flue worker and exercises the agent endpoint,
and PR previews deploy isolated per-PR workers with their own Durable Object state via new reusable
`preview-worker` actions (version uploads cannot carry DO migrations, and shared previews would
write into production's agent storage).

### feat(ravrun): training-plan engine, form UI, feasibility checks, ics export

ravrun generates real training plans now, replacing the hardcoded 18-week demo grid. A TDD-built
engine in `src/lib/plan/` (100% statement coverage) holds the logic: Riegel race prediction and
training pace bands, a race-date-anchored progressive-mileage generator (base → build → peak → taper
→ race phases, every-4th-week stepbacks, long runs climbing from current fitness to a
distance-specific peak, the week rotating around a chosen long-run day, pace bands drifting toward
goal fitness when the goal outruns the prediction), and a feasibility checker that flags
aggressive/unrealistic goals, unsafe mileage ramps (with a suggested duration that resolves its own
warning), too-short blocks, and already-underway windows. Every tunable training rule lives in one
`constants.ts` object so opinions are one-line edits guarded by anchor tests. The UI keeps its whole
config in URL search params (plans are shareable links), renders a color-coded grid on desktop and a
stacked agenda on phones, surfaces feasibility banners with a one-click "Use N weeks" fix, and
exports the plan as `.ics` all-day events. The custom domains ravrun.com and rav.run are now
declared in `wrangler.toml`, and `/about` explains the methodology.

### feat(tooling): adopt a changelog fragment workflow

Parallel PRs conflicted on every merge because each added its entry to the top of the current
`docs/changelog/YYYY-MM.md`. PRs now add one fragment file each under `docs/changelog/fragments/`
(`YYYY-MM-DD-<type>-<scope>-<short-slug>.md`, body: one entry in the existing format), and the
monthly files are only ever written by the roll-up: `mise run changelog:rollup`
(`bin/changelog-rollup.ts`, bun, unit-tested) folds fragments into the right month -- creating the
month file or day heading as needed, days newest-first, filename order within a day, content
preserved byte-for-byte -- then deletes what it folded and refreshes the README file index. A weekly
`cron-changelog-rollup` Depot workflow runs the task and opens a PR with the result (authenticating
through the repo's GitHub App so the PR still triggers CI); the task also runs manually.
CONTRIBUTING, the changelog README, the PR template, and the AGENTS hard rule now point at
fragments; this entry is the first user of the new flow.

## 2026-07-02

### chore(tooling): format repo-owned Markdown and gate it in CI

Executes the standing "format everything" decision from `lint-format-loose-ends`: a one-time
Prettier reflow of 42 files (`docs/**` plus root `*.md`, including semantic-line-break-authored
notes), guarded by a new `format:md` root mise task (a bare `prettier --check .`) and a
path-filtered `ci-docs.yml` workflow. `.prettierignore` sets the scope: `apps/` stays with the
per-app format gates (a root sweep would bypass djf.io's semantic-line-break blog exclusion),
`.agents/` is excluded because `skills-lock.json` pins skill content hashes, and `.claude/` plus the
legacy dirs mirror the spell gate's exclusions. `.prettierrc.json` also gained the JS/TS house-style
options (`semi: false`, `singleQuote: true`, `bracketSpacing: false`) so code fences embedded in
docs format to the same style Biome enforces in source, removing the need for `prettier-ignore`
guards around the style guide's own examples. This closes the papercut where adding a changelog
entry reflowed the whole file.

### chore(tooling): start enforcing the code style guide via oxlint

First tranche of mechanical enforcement for the new code style guide, wired into the existing
per-app oxlint gate through the root `.oxlintrc.json`. Rules verified zero-violation across every
app and `bin/` before enabling, so nothing turns red: `max-params` (max 3, the named-arguments
floor), `typescript/consistent-type-imports`, and — via the newly enabled `vitest` plugin —
`vitest/no-hooks` plus `vitest/max-nested-describe` (max 0), which make testing.md's no-describe /
no-hooks rules machine-checked. `typescript/consistent-type-definitions` (interface over type) ships
at `warn`: ~45 findings remain (heaviest in forzamonica.com, djf.io, f311x), tracked in the
`lint-format-loose-ends` project for a fix-up pass before promotion to `error`.

Enabling the vitest plugin surfaced 13 real correctness findings, fixed rather than silenced: every
app's `sentry-tunnel.test.ts` copy typed its fetch stub (`vi.fn<typeof fetch>`), and djf.io's
`content.config.test.ts` dropped a conditional `expect` by switching `safeParse` +
`if (result.success)` to a plain `parse`. Rust enforcement stays scoped out per the standing
decision, now with sizing data on record (~48 clippy-pedantic findings across the AoC 2022 crates).
The un-lintable style rules (classes must earn their place, pipelines over loops) remain with the
`engineering-reviewer` persona.

### docs(repo): add a code style guide

New `docs/contributing/code-style.md` documents how code is shaped, complementing the existing
which-language (scripting-style) and which-tool (tooling-standard) guides. It codifies: formatters
own formatting, with language norms first and house defaults (two-space, tight spacing, no
semicolons, single quotes, K&R braces) filling the gaps -- the JS/TS side of which was already live
in `biome.jsonc`; functional, data-centric shape (immutable data plus co-located free functions;
classes only when they earn it; pipelines over loops unless performance says otherwise); named
position-independent arguments past two params, with the `XxxParams`/`XxxProps`/`XxxOptions` naming
convention; TypeScript `interface`-over-`type` with the `Prettify<T>` escape hatch, `import type`,
named exports, and shoehorn-not-`as` in tests; Rust clippy-pedantic-and-fix-everything with no
`unwrap` outside tests; and a hard bash budget of fewer than 10 trivial lines before a script must
become Bun/TypeScript (or a purpose-built Rust/Go utility for recurring workflows).

Alongside it: `testing.md` gains parameterized tests (`test.each` over copy-paste, replacing
`describe` grouping), given/when/then body order, and property-based/fuzz guidance (fast-check,
proptest); `scripting-style.md`'s bash section now carries the explicit 10-line budget; the guide is
indexed in `CONTRIBUTING.md` and summarized as a hard rule in `AGENTS.md`.

### feat(forzamonica.com): grow the storefront site map and shared layout

The storefront's map was four routes (home hero, `/products`, `/products/$handle`, `/cart`) with a
two-link header and no footer. This pass fills out the pages a shop needs before launch and the
chrome that binds them: an `/about` placeholder (brand direction is still open, so the copy says
so), three policy stubs (`/policies/shipping`, `/policies/returns`, `/policies/privacy`) sharing a
`PolicyArticle` shell, and a styled 404 via the root route's `notFoundComponent` -- SSR'd with a
real 404 status, verified by e2e. Catalog stays deliberately flat: collections routes were
considered and deferred until the product line exists.

Layout-wise the root route gained a proper `component` (header / flex-grow main / footer inside the
existing shell), a new three-column `SiteFooter`, an About link in the header, and a cart-count
badge fed by a root-route loader calling a new badge-sized `fetchCartQuantity` server function -- no
cookie means no Shopify round trip, and every cart mutation already ends in `router.invalidate()`,
which re-runs the loader and keeps the badge honest. Every page now sets its own title (the PDP from
loader data).

The e2e suite grew from 2 to 6 tests (footer sitemap, about, policies, 404 status + chrome) with
header/footer-scoped selectors now that link names repeat, and the home visual baseline was
re-authored for the new chrome. `playwright.config.ts` gained a `PLAYWRIGHT_CHROMIUM_EXECUTABLE`
escape hatch for sandboxes that ship a system Chromium instead of allowing the pinned download; CI
is unaffected.

## 2026-07-01

### fix(ci): stop preview workflows duplicating their sticky PR comment

The preview-comment upsert only read the first page of a PR's comments
(`GET /issues/{n}/comments?per_page=100`). That endpoint returns comments oldest-first and has no
sort option, so once a PR crossed ~100 comments the sticky comment fell off page 1, the marker was
never found, and every rerun posted a fresh duplicate. `bin/comment-preview.ts` now walks every
page, collecting all marker-bearing comments; it edits the oldest in place and deletes the rest,
which both fixes forward and cleans up duplicates earlier runs already left on open PRs. Duplicate
DELETEs treat a 404 as a no-op so concurrent runs racing on the same stale comment don't abort the
cleanup.

Consolidated the two copies while here: `apps/f311x/bin/comment-preview.ts` was a near-identical
fork, so `cd-preview-f311x.yml` now calls the shared root script (via `$GITHUB_WORKSPACE`) with
`PREVIEW_MARKER` / `PREVIEW_TITLE` / `PREVIEW_FOOTER`, matching how the `preview-wrangler` composite
already drives it. The new `PREVIEW_FOOTER` input preserves f311x's Alchemy-stage teardown wording;
the wrangler apps keep the default version phrasing. One script, one place to fix, all 11 preview
workflows covered.

### fix(f311x): bump wrangler to 4.105.0 to satisfy vite-plugin peer dependency

Lockfile maintenance advanced `@cloudflare/vite-plugin` to 1.42.3, whose `wrangler` peer dependency
is `^4.105.0`, but the app still pinned `wrangler` at 4.103.0 -- so `vite build` failed CI with "The
installed version of Wrangler does not satisfy the peer dependency required by
@cloudflare/vite-plugin (^4.105.0)". Pinned `wrangler` to 4.105.0 (the minimum satisfying version)
and regenerated the lockfile.

### docs(repo): consolidate agent entry points and add review personas

Agent access had sprawled: `CLAUDE.md` carried ~180 lines duplicating `AGENTS.md`,
`CONTRIBUTING.md`, and the loose style guides at the `docs/` root, and the copies had already
drifted (CONTRIBUTING still described the retired underscore workflow naming). The path is now
`CLAUDE.md` (thin shim importing `AGENTS.md`) -> `AGENTS.md` (canonical entry: key paths, hard
rules, doc map) -> `CONTRIBUTING.md` (PR workflow + guide index) -> `docs/contributing/` (the mini
guides). Moved `configuration-style.md`, `scripting-style.md`, `github-actions-style.md`, and
`tooling-standard.md` into `docs/contributing/` via `git mv`; extracted `file-naming.md`,
`testing.md`, `project-docs.md`, and `linting.md` from the old `CLAUDE.md`/`CONTRIBUTING.md` prose;
folded the ecosystem tool choices and the `mise.lock` rule into `tooling-standard.md`.
`docs/agents/issue-tracker.md` is now the canonical home of the human-task issue format.

Added six review personas as `.claude/agents/` subagents -- `design-reviewer`, `product-reviewer`,
`engineering-reviewer`, `testing-reviewer`, `benchmark-reviewer`, `tooling-reviewer` -- each a
one-question rubric with explicit "not yours" boundaries and a capped report ("just enough" applies
to reviews too). A new `/panel-review` skill pins the diff, seats the relevant personas, fans them
out in parallel, and reports verdicts side by side without re-ranking them; `/review` keeps
standards/spec conformance.

### chore(repo): make .agents/ the solid home for all skills and review personas

The vendored mattpocock skills already followed the pattern -- solid copies in `.agents/skills/`,
tracked symlinks in `.claude/skills/` -- but the repo-authored surfaces did not. Moved
`dependency-freshness`, `panel-review`, and the six review personas into `.agents/` and left
per-item symlinks behind (`.claude/skills/<name>` -> skill folder, `.claude/agents/<name>.md` ->
agent file), so `.agents/` is now the tool-agnostic source of truth and `.claude/` is purely the
Claude Code discovery surface. Per-item symlinks are the documented-supported form and were verified
in-session (skills hot-register and personas spawn through them); whole-directory symlinks have
known upstream regressions and are avoided. No configuration alternative exists -- Claude Code has
no setting to relocate skill/agent discovery, and the plugin route would impose `plugin:name`
namespacing. Conventions recorded in `AGENTS.md`.

### chore(tooling): add tokenmaxxing to the cspell dictionary

Main picked up a draft blog post using the coinage, which turned the repo-wide spell gate red --
including on every PR, since the gate checks the merge result. Added the word alongside the list's
other coinages so the gate reflects intent.

### fix(f311x): repair the wip github.ts CI-credentials stack

The wip Alchemy stack pinned Stack's generics as `<void, ConfigError>`, but Stack's second type
parameter is the provider requirements, not the error channel (ConfigError is already hardwired in
the signature) -- so tsgo failed the whole app. Dropped the explicit generics so inference wires the
requirements from the merged Cloudflare + GitHub providers, and reformatted to house style. The file
stays inside the tsconfig include set: unlike `alchemy.run.ts` it typechecks cleanly, so it keeps
real coverage from the tsgo gate.
