# 2026-05

## 2026-05-13

### docs(repo): file pending human-intervention issues and remove draft file

Filed the four drafted human-intervention issues against `DavidJFelix/DavidJFelix` once the GitHub MCP server was reachable: install Warp CLI (#93), create Sentry org+projects (#94), create PostHog org+projects (#95), davidjfelix.com DNS cutover (#96). Deleted `docs/pending-issues.md` — issues are the durable record.

### docs(repo): codify human-intervention task convention; draft 4 pending issues

Added a "Human Intervention Tasks" section to `CLAUDE.md` specifying the required shape (background, checkbox steps, automation follow-up, related links) for any task that needs hands on a local machine, credentials, or third-party account decisions. Scanned project plans and drafted four such issues into `docs/pending-issues.md` to file once the GitHub MCP server was back.

### docs(repo): move reading list out of issues into `docs/reading-list.md`

Fifteen open issues labeled "on shelf" were reading-list todos, not actionable work. Closed all of them and consolidated into a single checklist doc, which is the right shape for this kind of running list.

## 2026-05-06

### docs(djf.io): align home page About section with refreshed README bio

Mirrored the structure and copy from the GitHub profile bio refresh onto `apps/djf.io/src/pages/index.astro`: narrative opener, Companies / Founded / Projects sections with date ranges, and a flat list of earlier roles & community work. Added explicit margins to About h4s since PandaCSS preflight had collapsed the spacing between role titles and their descriptions.

## 2026-05-05

### fix(djf.io, profile): remove links to defunct website

Stripped the link and link formatting pointing to a now-defunct site from both `README.md` and `apps/djf.io/src/pages/index.astro`.

### profile: refresh GitHub profile bio (`README.md`)

Replaced the role-history laundry list with a hybrid structure: narrative opener, visible Companies / Founded / Projects sections, and merged earlier roles & community work into Projects. Era Software and ServiceNow Cloud Observability (Lightstep) collapsed into one entry to reflect the acquisition continuity. MG4.tv reclassified as a project rather than a company. Current role at Brain-CA Technologies described by the work (AI research, MLOps, hardware engineering, team management) rather than an unofficial title. Date ranges filled in for all entries. Fixed typo (`applicaiton`). Closed out `docs/projects/profile-bio-refresh/`.

## 2026-05-03

### docs(repo): prune completed projects and split blog-migration follow-ups

Audited active projects against the changelog and repo state. Closed out four projects whose work is complete and captured in prior changelog entries: blog-migration (core; 2026-02-06), cloudflare-migration (2026-04-30), dev-tooling-consolidation (2026-02-21), and djf-io-testing (2026-04-29). Extracted blog-migration's two unfinished phases into their own projects: djf-io-search (Pagefind) and djf-io-seo (SEO meta, OG images, sitemap, Lighthouse).

### docs(repo): establish project lifecycle policy

Documented in `CONTRIBUTING.md`, `AGENTS.md`, and `CLAUDE.md` that `docs/projects/<name>/` directories are ephemeral working notes. When a project completes, capture it in the changelog, delete the directory, and remove the entry from `docs/projects.md`. The changelog is the durable record.

### docs(repo): close out contribution-docs project

Reframed `AGENTS.md` sub-folder agent docs as optional/as-needed. Documented in `CONTRIBUTING.md` and `docs/changelog/README.md` that months without changes are skipped (no empty files). Deleted `docs/projects/contribution-docs/` and removed the entry from `docs/projects.md`.
